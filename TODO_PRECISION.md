- [x] Implementare serie perturbativa ELP2000-82B indipendente per perigeo in `_calc_elp2000_perigee_perturbations()` e `calc_interpolated_perigee()` at libephemeris/lunar.py:1220,2611. PROBLEMA: Il calcolo perigeo usa coefficienti calibrati su dati Swiss Ephemeris ma restituisce errori max 20°, mean 12° perche la serie perturbativa attuale non cattura completamente le asimmetrie solari sul perigeo rispetto all'apogeo, dove le perturbazioni hanno ampiezza 5x maggiore (~25° vs ~5°). IMPATTO: Calcoli supermoon/perigeo lunare inaffidabili per applicazioni astrologiche che richiedono timing preciso, previsioni marea massima errate, correlazioni perigeo-sismicita compromesse per ricercatori. FIX: Ricalcolare coefficienti serie perigeo con regressione least-squares su 2000+ punti SE distribuiti uniformemente 1900-2100, aggiungere termini di ordine superiore (4D-2M', 4D-4M', 2D-M, 2D-M-2F) e validare con RMS target <1°. RISULTATO ATTESO: Errore interpolated perigee ridotto da max 20° a <3°, mean da 12° a <1°, rendendo calcoli supermoon affidabili con precisione sufficiente per applicazioni astrologiche professionali.

- [x] Implementare metodo analitico Moshier per apogeo interpolato in `_calc_elp2000_apogee_perturbations()` e `calc_interpolated_apogee()` at libephemeris/lunar.py:1120,2257. PROBLEMA: Il metodo attuale usa serie perturbativa ELP2000-82B con ~15 termini calibrati empiricamente, ma Swiss Ephemeris usa teoria lunare analitica completa di Moshier che include ~50 termini armonici per la longitudine apsidale, causando errore max 2°, mean 1° sistematico non riducibile con tuning parametri. IMPATTO: Lilith "naturale" (interpolated apogee) differisce significativamente da software astrologico di riferimento, creando discrepanze in transiti Lilith, aspetti natali, e confronti tra software diversi. FIX: Estrarre i ~50 termini dominanti dalla teoria lunare Moshier per il argomento del perigeo (omega), implementare come serie armonica analitica anziche regressione su dati osculanti. RISULTATO ATTESO: Errore interpolated apogee ridotto da max 2° a <0.5°, mean da 1° a <0.2°, raggiungendo compatibilita pratica con Swiss Ephemeris per applicazioni Lilith.

- [x] Completare implementazione TASS 1.7 con tutti i termini periodici in `_titan_position()` e `saturn_moon_position()` at libephemeris/moon_theories/tass17.py:74,99. PROBLEMA: Solo Titan (satellite 5) e implementato con formula semplificata (~5 termini), mentre la teoria TASS 1.7 completa richiede ~2000 termini periodici per satellite. Gli altri 7 satelliti (Mimas, Enceladus, Tethys, Dione, Rhea, Hyperion, Iapetus) restituiscono (0,0,0), precisione Titan attuale ~500km vs target 50km. IMPATTO: Correzione baricentro Saturno incompleta (manca 4% contributo altri satelliti), posizioni satelliti saturno non disponibili per occultazioni/transiti, ricerca astronomica su sistema Saturno impossibile. FIX: Portare implementazione completa TASS 1.7 da Stellarium (MIT license, ~2000 termini per satellite), includere tutti 8 satelliti maggiori, validare vs JPL Horizons. RISULTATO ATTESO: Precisione Titan migliorata da ~500km a ~50km (~0.05 arcsec a distanza Saturno), tutti 8 satelliti disponibili con precisione sub-arcsecond, correzione baricentro completa.

- [x] Allineare elementi orbitali pianeti uraniani con fonte Swiss Ephemeris in `URANIAN_ELEMENTS` e `UranianElements` at libephemeris/hypothetical.py:132,160-217. PROBLEMA: Gli elementi orbitali per gli 8 pianeti uraniani (Cupido, Hades, Zeus, Kronos, Apollon, Admetos, Vulkanus, Poseidon) differiscono da Swiss Ephemeris, causando errori longitudine 0.1°-1° e latitudine fino a 5°. Intero modulo test marcato xfail. IMPATTO: Software astrologia Hamburg School produce risultati incompatibili con riferimenti standard, grafici planetenbild errati, professionisti costretti a usare software alternativo. FIX: Estrarre coefficienti esatti (L0, n, amplitude, phase, phase_rate) da sorgente Swiss Ephemeris seorbel.txt, validare calcoli su range 1900-2100 con tolleranza <0.01°. RISULTATO ATTESO: Errore pianeti uraniani ridotto a <0.05° longitudine e <0.5° latitudine, test module hypothetical passa invece di xfail, compatibilita Hamburg School raggiunta.

- [x] Implementare modello atmosferico Schaefer completo per eventi eliacali in `heliacal_ut()` e `vis_limit_mag()` at libephemeris/heliacal.py:100,1377. PROBLEMA: Algoritmo visibilita eliacale usa modello semplificato sky brightness/extinction che differisce da implementazione SE basata su Schaefer (1990), causando errori timing 1-2 giorni per pianeti e fino a 2 giorni per stelle. Intero modulo test marcato xfail. IMPATTO: Calcoli alba/tramonto eliacale inaffidabili per archeologia astronomica, calendari antichi (es. sorgere eliacale Sirio per Egizi), ricerca storica su visibilita planetaria. FIX: Implementare modello Schaefer (1990) completo con: estinzione atmosferica Rayleigh+aerosol, sky brightness lunare/crepuscolare, soglie visibilita Ptolemaiche, stessi parametri/costanti usati da SE. RISULTATO ATTESO: Errore timing eventi eliacali ridotto da 1-2 giorni a <0.5 giorni, test module heliacal passa invece di xfail, compatibilita con Swiss Ephemeris per applicazioni storiche.

- [x] Aggiungere termini ordine superiore per True Node date storiche in `_calc_elp2000_node_perturbations()` at libephemeris/lunar.py:298. PROBLEMA: Per date pre-1800 CE, errori True Node eccedono tolleranza 0.15° perche polinomi Meeus degradano come T^4 e serie perturbativa ELP2000-82B usa solo ~25 termini. Test storici (eclissi 1500-1800) marcati xfail. IMPATTO: Nodo lunare per oroscopi storici impreciso, ricerca astrologica su figure storiche compromessa, validazione eclissi antiche contro record storici fallisce. FIX: Aggiungere termini T^4 e T^5 ai polinomi Meeus, includere 10-15 termini ELP2000 addizionali per elongazioni alte (5D, 6D), calibrare su eclissi storiche documentate. RISULTATO ATTESO: Errore True Node per date 1500-1800 ridotto sotto 0.15°, test historical eclipse passa invece di xfail, calcoli nodo affidabili fino a 1000 CE.

- [x] Unificare algoritmo linea centrale eclissi in `calc_eclipse_central_line()` at libephemeris/eclipse.py:11826. PROBLEMA: Funzione `calc_eclipse_central_line()` usa algoritmo diverso da `sol_eclipse_where()` per trovare coordinate linea centrale, causando discrepanze ~1°. Test comparativo marcato xfail. IMPATTO: Path eclissi solari totali impreciso per pianificazione osservazioni, turismo astronomico riceve coordinate errate, cartografia eclissi non corrisponde a realta. FIX: Usare approccio Besseliano consistente in entrambe funzioni, calcolare intersezione cono ombra con ellissoide terrestre usando stessi parametri geometrici. RISULTATO ATTESO: Discrepanza linea centrale ridotta da ~1° a <0.1°, test calc_eclipse_central_line passa invece di xfail, coordinate path eclissi accurate per navigazione.

- [x] Implementare modello librazione risonante per plutini in `calc_minor_body_position()` dopo `detect_mean_motion_resonance()` at libephemeris/minor_bodies.py:358,922. PROBLEMA: `detect_mean_motion_resonance()` identifica correttamente plutini (Ixion, Orcus in 2:3 Neptune) ma il calcolo posizione usa solo perturbazioni secolari Laplace-Lagrange, ignorando oscillazioni librazione risonante con periodo ~20000 anni e ampiezza variabile per oggetto. Errori crescono 5-10° su archi 50+ anni. IMPATTO: Posizioni TNO generazionali (Ixion, Orcus, Pluto) inaffidabili per efemeridi multi-decennali, ricerca astrologica generazionale compromessa, previsioni transiti plutini errate. FIX: Implementare modello librazione semplificato usando argomento risonante phi=3*lambda_TNO-2*lambda_Neptune-omega_TNO, aggiungere correzione oscillatoria calibrata per-oggetto da dati JPL Horizons. RISULTATO ATTESO: Errore posizione plutini ridotto da 5-10° a <2° su archi 100 anni, efemeridi TNO affidabili per XXI secolo, ricerca astrologica generazionale possibile.

- [x] Correggere segno velocita latitudine stelle fisse in `calc_fixed_star_velocity()` at libephemeris/fixed_stars.py:3388. PROBLEMA: Il segno della velocita latitudine per stelle fisse differisce da Swiss Ephemeris a causa di algoritmo proper motion diverso nella propagazione coordinate. Test velocita stelle marcato xfail. IMPATTO: Software che usa velocita stelle per animazioni cielo o calcoli avanzati mostra direzione moto errata, inconsistenza con altri software astronomici. FIX: Allineare convenzione segno con SE, verificare trasformazione coordinate equatoriali->eclittiche nel calcolo proper motion, testare su stelle ad alto moto proprio (Barnard, Proxima). RISULTATO ATTESO: Segno velocita latitudine stelle fisse concorda con Swiss Ephemeris, test fixed_star_velocity passa invece di xfail, animazioni moto proprio corrette.

- [x] Documentare differenza metodologica nod_aps come intenzionale in `_calc_nod_aps()` at libephemeris/planets.py:3050. PROBLEMA: Funzioni nod_aps usano elementi orbitali medi eliocentrici (Standish 1992 JPL/IERS) mentre SE usa interpretazione geocentrica, causando differenze fino a 250° per pianeti interni (Mercurio, Venere). Test marcato xfail ma differenza e metodologica, non bug. IMPATTO: Utenti pensano ci sia errore quando confrontano con SE, issue reports inutili, confusione su quale risultato sia "corretto". FIX: Aggiungere docstring dettagliata spiegando differenza metodologica, aggiungere warning se utente richiede nod_aps per pianeti interni, documentare in PRECISION.md. RISULTATO ATTESO: Utenti comprendono che differenza e intenzionale, documentazione spiega quando usare quale interpretazione, issue reports metodologici eliminati.

- [x] Refactoring type signature `lon_or_hsys` per type safety in `house_pos()` e `swe_house_pos()` at libephemeris/houses.py:4043,4346. PROBLEMA: Parametro `lon_or_hsys: Optional[Union[float, bytes, str]]` causa errori LSP/mypy residui (linee 4108, 4118, 4270-4311) perche Union type complesso non gestito correttamente in tutti i branch, IDE mostra warning continui. IMPATTO: Developer experience degradata, warning LSP distraggono, potenziali bug runtime se type coercion fallisce su input inaspettato. FIX: Usare overload function signatures per separare casi (float lon vs str hsys), o creare type guard function per disambiguare, aggiornare tutti branch per gestire ogni tipo esplicitamente. RISULTATO ATTESO: Zero errori LSP/mypy in houses.py, type safety completa per house_pos, IDE autocomplete funziona correttamente.

- [x] Implementare calcolo giorno/notte 3D per Arabic Parts in `is_day_chart()` at libephemeris/arabic_parts.py:145. PROBLEMA: Funzione usa confronto longitudine eclittica 2D (sun_lon vs asc) che e impreciso a latitudini estreme dove piano orizzonte interseca eclittica in modo complesso, potenzialmente invertendo determinazione giorno/notte. IMPATTO: Parti arabe calcolate con formula sbagliata (day vs night) per localita nordiche/antartiche, errori fino a 180° per parti come Part of Fortune. FIX: Implementare calcolo 3D usando coordinate orizzontali Sole (altitudine sopra/sotto orizzonte), con fallback a metodo 2D per latitudini moderate. RISULTATO ATTESO: Determinazione giorno/notte corretta a tutte le latitudini incluse polari, parti arabe accurate globalmente, nessun edge case a latitudini estreme.

- [x] Aggiungere warning per date oltre range ottimale Meeus in `calc_mean_lilith()` e funzioni lunar at libephemeris/lunar.py:140-145. PROBLEMA: Costanti MEEUS_OPTIMAL_CENTURIES, MEEUS_VALID_CENTURIES, MEEUS_MAX_CENTURIES definite ma non usate per warning utente. Calcoli silenziosamente degradano da <0.001° (±200yr) a errori significativi (±2000yr) senza notifica. IMPATTO: Utenti ignari ricevono risultati imprecisi per date storiche remote, ricerca su civilta antiche usa dati degradati, nessuna indicazione di affidabilita risultato. FIX: Emettere warning Python se |T| > MEEUS_OPTIMAL_CENTURIES, raise exception o warning severo se |T| > MEEUS_MAX_CENTURIES, documentare range affidabilita in docstring. RISULTATO ATTESO: Utenti informati quando precisione degrada, warning esplicito per date oltre ±200 anni, exception per date oltre ±2000 anni, trasparenza su limiti calcolo.

- [x] Migliorare precisione timing occultazioni lunari in `lun_occult_when_glob()` e `lun_occult_when_loc()` at libephemeris/eclipse.py:4969,5370. PROBLEMA: Timing occultazioni lunari ha tolleranza 300 secondi (5 minuti) vs Swiss Ephemeris, causato da differenze nel calcolo parallasse topocenrica e algoritmo ricerca contatti. Test usa TIME_SECONDS=300.0 che e molto rilassato per applicazioni osservative. IMPATTO: Previsioni occultazioni stellari/planetarie imprecise per osservatori, timing disappearance/reappearance errato di 5+ minuti, pianificazione osservazioni astronomiche compromessa. FIX: Implementare calcolo parallasse lunare completo con ellissoide WGS84, usare golden section search per contatti come in eclissi solari, ridurre step di ricerca iniziale. RISULTATO ATTESO: Timing occultazioni ridotto da 300s a <60s tolleranza, previsioni affidabili per osservatori amatoriali, compatibilita con software occultazioni dedicati.

- [x] Migliorare precisione alba/tramonto/transito in `rise_trans()` at libephemeris/eclipse.py:6358. PROBLEMA: Funzioni rise/transit/set hanno tolleranza 120 secondi (2 minuti) vs Swiss Ephemeris, causato da differenze in modello rifrazione atmosferica e altezza orizzonte apparente. Test usa TIME_TOL_SECONDS=120.0. IMPATTO: Orari alba/tramonto imprecisi per applicazioni che richiedono timing esatto (fotografia, navigazione, orari preghiera), transiti meridiani errati per calibrazione strumenti. FIX: Allineare modello rifrazione con SE (Bennett formula), usare stessa definizione di orizzonte geometrico/apparente, implementare correzione semidiametro solare/lunare identica. RISULTATO ATTESO: Timing rise/set ridotto da 120s a <30s tolleranza, orari alba/tramonto affidabili per applicazioni pratiche, compatibilita con almanacchi nautici.

- [x] Implementare formula magnitudine Plutone accurata in `swe_pheno_ut()` at libephemeris/planets.py:3980,4208. PROBLEMA: Formula magnitudine Plutone usa coefficienti approssimati (`SE_PLUTO: (-1.00, 0.041, 0.0, 0.0)` con commento "approximate"), tolleranza test 1.0 magnitudine che e molto rilassata per oggetto gia debole (mag ~14). IMPATTO: Magnitudine Plutone errata di ~1 mag significa errore 2.5x in luminosita, previsioni visibilita telescopica inaffidabili, confronti con osservazioni reali falliscono. FIX: Implementare formula magnitudine Plutone da Astronomical Algorithms (Meeus) o IAU, includere correzione fase e albedo variabile, calibrare su dati osservativi recenti. RISULTATO ATTESO: Magnitudine Plutone accurata a ±0.2 mag, previsioni visibilita affidabili per osservatori, coerenza con database magnitudini planetarie.

- [x] Implementare modello magnitudine Luna Hapke in `swe_pheno_ut()` sezione Moon at libephemeris/planets.py:4208. PROBLEMA: Commento esplicito "Moon's magnitude (simplified formula)" indica che calcolo magnitudine lunare usa formula semplificata invece di modello fotometrico Hapke che tiene conto di scattering superficiale, angolo fase, e librazione. Errori possono raggiungere 0.5 mag. IMPATTO: Magnitudine Luna imprecisa influenza calcoli sky brightness per visibilita stelle/pianeti, modelli estinzione atmosferica ricevono input errato, heliacal events timing degradato. FIX: Implementare modello Hapke semplificato con parametri lunari standard, includere correzione opposizione surge, validare contro dati fotometrici USNO. RISULTATO ATTESO: Magnitudine Luna accurata a ±0.1 mag su tutto il range fasi, calcoli sky brightness migliorati, heliacal events piu precisi.

- [x] Correggere convenzione segno ayanamsha J2000 per date storiche in `_get_ayanamsha_value()` at libephemeris/planets.py:2586. PROBLEMA: Test J2000 ayanamsha a 1900 marcato xfail perche "sign convention differs at 1900" - libephemeris e SE usano convenzioni opposte per il segno dell'ayanamsha quando la data e prima dell'epoca J2000, causando valori con segno invertito. IMPATTO: Calcoli siderali per date storiche (pre-1900) producono risultati con segno errato, ricerca astrologica storica compromessa, incompatibilita con software di riferimento. FIX: Verificare convenzione SE per ayanamsha negativa (precessione all'indietro), allineare calcolo J2000 per restituire valore negativo prima di J2000.0 e positivo dopo, aggiornare test. RISULTATO ATTESO: Ayanamsha J2000 corretta per tutte le date, segno negativo prima del 2000 e positivo dopo, test J2000 passa invece di xfail.

- [x] Migliorare precisione ayanamshe galactic center in calcoli `SE_SIDM_GALCENT_*` at libephemeris/planets.py:2492-2586. PROBLEMA: Le 4 ayanamshe galactic center (GALCENT_0SAG, GALCENT_RGILBRAND, GALCENT_MULA_WILHELM, GALCENT_COCHRANE) hanno tolleranza test rilassata a 1° invece di 0.001° standard, indicando discrepanze significative con Swiss Ephemeris nel calcolo posizione centro galattico. IMPATTO: Astrologi che usano sistemi galattocentrici ricevono risultati incompatibili con altri software, ricerca su allineamenti galattici imprecisa, tradizioni astrologiche galattiche non supportate correttamente. FIX: Verificare coordinate J2000 centro galattico usate (Sgr A*: RA 17h45m40s, Dec -29°00'28"), allineare modello precessione galattica con SE, calibrare offset per ogni variante. RISULTATO ATTESO: Ayanamshe galactic center accurate a <0.01°, compatibilita con Swiss Ephemeris, supporto corretto per tradizioni galattocentriche.

- [x] Migliorare precisione ayanamshe star-based True Revati/Pushya/Mula in `_get_ayanamsha_value()` at libephemeris/planets.py:2480-2488. PROBLEMA: Ayanamshe TRUE_REVATI, TRUE_PUSHYA, TRUE_MULA hanno tolleranza 0.06° (~216 arcsec) invece di 0.006° per TRUE_CITRA, indicando algoritmo meno preciso per queste stelle di riferimento, probabilmente per mancanza parallasse/velocita radiale nei dati stellari. IMPATTO: Oroscopi Vedic con queste ayanamshe differiscono da software di riferimento, tradizioni che usano Revati/Pushya/Mula come riferimento ricevono risultati imprecisi. FIX: Aggiungere dati Hipparcos completi (parallasse, RV, proper motion) per Zeta Piscium (Revati), Delta Cancri (Pushya), Lambda Scorpii (Mula), implementare propagazione proper motion completa come per Spica/True Citra. RISULTATO ATTESO: Ayanamshe True Revati/Pushya/Mula accurate a <0.01°, parita con True Citra, supporto completo tradizioni Vedic.

- [x] Risolvere dipendenza da data inizio per ricerca eclissi in `sol_eclipse_when_glob()` at libephemeris/eclipse.py:1141. PROBLEMA: Test August 2017 eclipse marcati xfail perche "eclipse search may find different eclipse depending on start date" - algoritmo di ricerca puo saltare eclissi vicine se la data di inizio e troppo prossima, restituendo eclisse successiva invece di quella target. IMPATTO: Utenti che cercano eclissi specifica possono ricevere eclisse sbagliata, automazione ricerca eclissi inaffidabile, validazione storica eclissi fallisce. FIX: Implementare ricerca bidirezionale che controlla entro ±15 giorni dalla data inizio, verificare che nessuna eclisse sia stata saltata, aggiungere parametro opzionale per forzare direzione ricerca. RISULTATO ATTESO: Ricerca eclissi affidabile indipendentemente da data inizio, test August 2017 passa invece di xfail, nessuna eclisse saltata per edge cases.

- [x] Documentare fallback nutazione 4-termini come limitazione nota in `calc_nutation_iau2000a()` at libephemeris/planets.py:1067-1109. PROBLEMA: Se Skyfield non e disponibile, il calcolo nutazione degrada da IAU2000A (1365 termini, sub-milliarcsec) a modello semplificato 4-termini (~1 arcsec). Questo fallback e silenzioso e non documentato per utenti che potrebbero non avere Skyfield installato correttamente. IMPATTO: Utenti ignari ricevono precisione nutazione degradata di 1000x senza warning, calcoli che dipendono da nutazione (nodi, eclittica vera) hanno errori nascosti, debugging difficile. FIX: Emettere warning esplicito quando si usa fallback 4-termini, documentare in PRECISION.md e docstring, aggiungere funzione per verificare quale modello nutazione e attivo. RISULTATO ATTESO: Utenti informati quando nutazione usa fallback, warning chiaro se Skyfield manca, funzione `get_nutation_model()` per verificare stato.
