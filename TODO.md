- [ ] Implement ELP2000-82B apsidal perturbation series in `calc_interpolated_apogee()` at libephemeris/lunar.py:1833-2066. PROBLEM: Current polynomial regression over 56-day window (line 1980) uses osculating elements from JPL DE which differ fundamentally from Swiss Ephemeris's Moshier analytical lunar theory, causing 8-10° systematic offset that cannot be reduced by parameter tuning. IMPACT: Astrological software relying on SE_INTP_APOG compatibility will show 8-10° discrepancy, affecting Lilith-based aspect calculations, transit predictions, and natal chart interpretations for practitioners using interpolated apogee. FIX: Implement analytical apsidal longitude computation using ELP2000-82B perturbation series coefficients for lunar argument of perigee (ω), similar to existing `_calc_elp2000_node_perturbations()` pattern at line 992, extracting mean apsidal position plus ~50 largest periodic terms. EXPECTED RESULT: Reduce interpolated apogee error from 8-10° to <2° vs Swiss Ephemeris, achieving practical compatibility for astrological applications.

- [ ] Compute perigee independently from apogee in `_sample_osculating_perigee_with_fallback()` at libephemeris/lunar.py:2286,2311,2322. PROBLEM: Current implementation computes perigee as `apogee_lon + 180.0` (lines 2286, 2311, 2322), but Swiss Ephemeris documentation states apogee and perigee are NOT exactly opposite—they deviate by up to 28° depending on Sun-Moon geometry, only being roughly opposite when Sun is in conjunction or at 90° angle. IMPACT: Interpolated perigee shows 10-15° error vs Swiss Ephemeris, larger than apogee error, because the 180° assumption compounds with the underlying osculating calculation differences. FIX: Compute perigee eccentricity vector independently by finding the +e direction (perigee) directly from orbital mechanics rather than negating apogee direction, using separate sampling and interpolation pipeline for perigee longitude. EXPECTED RESULT: Reduce interpolated perigee error from 10-15° to <3°, matching Swiss Ephemeris behavior where apogee/perigee separation varies dynamically.

- [ ] Update True Lilith precision documentation in docs/PRECISION.md:280,326. PROBLEM: Documentation states True Lilith has "~5-15°" error with "occasional differences up to 25°" (lines 280, 326), but actual testing shows mean error of 52 arcsec (0.015°) and max error of 235 arcsec (0.065°)—documentation is outdated by ~300x and misleads users about library capabilities. IMPACT: Users may avoid True Lilith calculations or choose Mean Lilith unnecessarily, missing the high-precision osculating apogee that libephemeris actually provides; library credibility suffers from inaccurate documentation. FIX: Update line 280 to show "~0.02° (~70 arcsec)" and rewrite the "Note on True Lilith" section at line 326 to reflect the eccentricity vector method achieves sub-arcminute precision, with max errors under 0.1° across 1950-2050 range. EXPECTED RESULT: Documentation accurately reflects True Lilith precision of ~52 arcsec mean / ~235 arcsec max, guiding users to appropriate calculation method.

- [ ] Add higher-order polynomial terms to `calc_mean_lilith()` formula at libephemeris/lunar.py:1483. PROBLEM: Current Meeus polynomial (line 1483) uses only T³ terms: `83.3532465 + 4069.0137287*T - 0.0103200*T² - T³/80053`, but Swiss Ephemeris uses additional T⁴ and periodic correction terms, causing systematic drift of ~270 arcsec (0.075°) mean error that grows with time from J2000. IMPACT: Mean Lilith positions diverge from Swiss Ephemeris reference, particularly for historical dates before 1900 and future dates after 2100, affecting long-term astrological research and ephemeris generation. FIX: Add T⁴ term from Swiss Ephemeris source (`+ T⁴/3526000`) and implement periodic corrections for solar/lunar perturbations on mean apsidal motion, calibrating coefficients against SE reference across 1800-2200 range. EXPECTED RESULT: Reduce Mean Lilith error from 270 arcsec to <100 arcsec, improving long-term accuracy for historical and predictive applications.

- [ ] Commit ELP2000-85 node perturbation terms added to libephemeris/lunar.py:1066-1113. PROBLEM: Uncommitted changes add 25+ ELP2000-85 refinement terms (fifth-order elongation, triple combinations, planetary cross-coupling, higher F harmonics, secular T² terms) that improve True Node precision by ~5-10 arcsec, but these improvements are not in version control and could be lost. IMPACT: Development work improving True Node calculation is at risk of loss; other contributors cannot review or build upon these improvements; CI/CD pipelines test against older code. FIX: Review the 48-line diff adding higher-order terms, verify test suite passes with `pytest tests/ -v`, then commit with message describing ELP2000-85 refinements and expected precision improvement. EXPECTED RESULT: ELP2000-85 terms committed to main branch, True Node precision improved by ~5-10 arcsec, changes preserved in git history.

- [ ] Commit central difference velocity improvements in libephemeris/planets.py. PROBLEM: Uncommitted changes (188 lines modified) upgrade velocity calculations from forward differences O(h) to central differences O(h²), providing ~100x better numerical precision for planetary velocities, but changes remain uncommitted and at risk of loss. IMPACT: Velocity precision improvements not available to users; potential merge conflicts if other changes touch same code; CI tests run against older forward-difference implementation. FIX: Review the velocity calculation changes in planets.py diff, run precision validation with `python compare_scripts/generate_precision_report.py`, verify velocity tolerances pass, then commit with descriptive message about central difference upgrade. EXPECTED RESULT: Central difference velocities committed, velocity precision improved from O(h) to O(h²), ~100x reduction in numerical differentiation error.

- [ ] Implement automatic SPK kernel download for major asteroids in libephemeris/minor_bodies.py:733+. PROBLEM: MINOR_BODY_ELEMENTS dictionary (line 733) provides Keplerian elements with secular perturbations achieving only ~1-10° precision, while JPL provides high-precision SPK kernels for Ceres, Vesta, Pallas, Juno that would give sub-arcsecond accuracy, but users must manually download and register these files. IMPACT: Users get degraded asteroid precision by default; manual SPK setup is error-prone and undocumented for most users; professional astrological software cannot rely on asteroid positions for precise timing. FIX: Implement `auto_download_asteroid_spk(body_id)` function that checks SPK cache, downloads from JPL Horizons if missing (similar to existing `download_and_register_spk()`), and automatically uses SPK when available, falling back to Keplerian only when SPK unavailable. EXPECTED RESULT: Major asteroids (Ceres, Vesta, Pallas, Juno, Chiron) achieve sub-arcsecond precision automatically, with transparent fallback to Keplerian for obscure bodies.

- [ ] Add mean motion resonance dynamics for plutinos in libephemeris/minor_bodies.py:302-395. PROBLEM: `detect_mean_motion_resonance()` function (line 302) identifies plutinos like Ixion and Orcus in 2:3 Neptune resonance, but `calc_minor_body_position()` (line 392) applies only secular perturbations without resonant libration modeling, causing 5-10° errors that grow over multi-decade timescales. IMPACT: TNO positions for plutinos drift from true positions over 50+ year spans; research-grade astrological work on generational objects becomes unreliable; ephemeris generation for 21st century shows accumulated errors. FIX: Implement simplified resonant libration model using resonant argument φ = 3λ_TNO - 2λ_Neptune - ω_TNO, adding oscillatory correction term with period ~20,000 years and amplitude calibrated per-object from JPL Horizons data. EXPECTED RESULT: Plutino position errors reduced from 5-10° to <2° over 100-year spans, enabling reliable generational astrology research.

- [ ] Implement proper motion velocity for fixed stars in `swe_fixstar_ut()` at libephemeris/fixed_stars.py:3537,3593,3817. PROBLEM: Fixed star functions return zero velocities (lines 3537, 3593, 3817 return `0.0, 0.0, 0.0` for speed components) even when SEFLG_SPEED is requested, despite having proper motion data available and `propagate_proper_motion()` function at line 217 that computes position changes over time. IMPACT: Applications requesting star velocities get zeros instead of proper motion rates; high proper motion stars like Barnard's Star show no apparent motion; velocity-dependent calculations (e.g., station detection algorithms) fail for stars. FIX: When SEFLG_SPEED flag is set, compute velocity by calling star position at t±0.5 days and using central difference: `speed_lon = (lon_next - lon_prev) / 1.0`, `speed_lat = (lat_next - lat_prev) / 1.0`, returning proper motion in deg/day. EXPECTED RESULT: Fixed star velocities reflect actual proper motion rates, Barnard's Star shows ~10.3 arcsec/year motion, enabling proper motion-aware applications.

- [ ] Improve True Node and Mean Lilith velocity calculations in libephemeris/lunar.py. PROBLEM: Node and Lilith velocity calculations use simple forward differences or analytical derivatives of mean motion formulas, but do not account for the perturbation terms that affect the osculating positions, causing velocity errors that can affect station detection and speed-based aspect calculations. IMPACT: Lunar node station calculations may be imprecise; Lilith speed for natal charts shows discontinuities; velocity-weighted aspect orbs computed incorrectly for these points. FIX: Implement central difference velocity calculation for True Node and True Lilith using the same perturbation-corrected position functions at t±0.5 days, ensuring velocity reflects actual rate of change including all periodic terms. EXPECTED RESULT: Node and Lilith velocities accurate to <0.001°/day, enabling precise station detection and velocity-based aspect calculations.

- [ ] Add Interpolated Apogee precision limitation note to docs/PRECISION.md:328. PROBLEM: Current documentation at line 328 explains the 8-10° difference but frames it as acceptable, without clearly warning users that this is a fundamental limitation requiring analytical method implementation to resolve, potentially misleading users about achievability of better precision through parameter tuning. IMPACT: Users may spend time trying to tune interpolation parameters expecting better results; library maintainers may receive bug reports about "incorrect" interpolated apogee; expectations mismatch between documentation and achievable precision. FIX: Add explicit warning: "NOTE: Achieving <2° precision requires implementing Moshier's analytical apsidal computation method. The current polynomial regression approach cannot bridge this gap regardless of parameter tuning. See TODO.md for implementation plan." EXPECTED RESULT: Users clearly understand interpolated apogee limitation, reducing confusion and setting appropriate expectations for SE compatibility.

- [ ] Update TRUE_LILITH_METHODS.md with current precision measurements at docs/TRUE_LILITH_METHODS.md. PROBLEM: The True Lilith methods documentation may contain outdated precision claims that do not reflect the current excellent performance of the eccentricity vector method (52 arcsec mean, 235 arcsec max), potentially confusing users researching which Lilith calculation to use. IMPACT: Users making method selection decisions based on outdated precision data; documentation inconsistency between PRECISION.md and TRUE_LILITH_METHODS.md after PRECISION.md update; incomplete picture of library capabilities. FIX: Review and update TRUE_LILITH_METHODS.md to include current precision measurements from 500-date random sampling, add comparison table showing True Lilith (0.015°) vs Mean Lilith (0.075°) vs Interpolated Apogee (8-10°), recommend True Lilith for osculating applications. EXPECTED RESULT: TRUE_LILITH_METHODS.md provides accurate, current precision data helping users select appropriate Lilith calculation method.

- [ ] Upgrade to DE440 ephemeris for improved outer planet precision in libephemeris configuration. PROBLEM: Library may still default to DE421 which has less observational data for outer planets compared to DE440 (released 2021) which includes 12+ additional years of spacecraft tracking data and uses ICRF 3.0 reference frame for improved accuracy. IMPACT: Outer planet positions (Uranus, Neptune, Pluto) have slightly higher errors than achievable; users wanting best precision must manually configure DE440; library not using latest available ephemeris data. FIX: Update default ephemeris configuration to prefer DE440 when available, with automatic fallback to DE421; update documentation to recommend DE440 download; verify precision improvements in test suite. EXPECTED RESULT: Outer planet precision improved by factor of 2-3x with DE440, library uses best available ephemeris by default.

- [ ] Use planet center instead of planet barycenter for gas giant calculations. PROBLEM: Current implementation may use planetary system barycenter (which includes moons) rather than planet center for Jupiter, Saturn, Uranus, Neptune, causing small but measurable offsets especially for Jupiter where Io/Europa/Ganymede/Callisto significantly offset the barycenter from Jupiter's center. IMPACT: Gas giant positions offset by up to several arcseconds from expected planet-center values; inconsistency with Swiss Ephemeris which uses planet centers; affects precise aspect calculations involving gas giants. FIX: Verify Skyfield/JPL DE queries use planet center NAIF IDs (599, 699, 799, 899) rather than barycenter IDs (5, 6, 7, 8); add explicit center selection in planet position calculations; validate against Swiss Ephemeris reference. EXPECTED RESULT: Gas giant positions accurate to planet center rather than system barycenter, sub-arcsecond agreement with Swiss Ephemeris.
