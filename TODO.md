## MOSHIER

# TODO: Implementazione Moshier Semi-Analytical Ephemeris

> Moshier è una modalità ESPLICITA attivata dal flag `SEFLG_MOSEPH` (4). NON è un fallback silenzioso.
> - `SEFLG_SWIEPH` / `SEFLG_JPLEPH` (default): usa DE440 via Skyfield. Fuori range 1550-2650 → `EphemerisRangeError`.
> - `SEFLG_MOSEPH`: usa algoritmi semi-analitici. Range -3000 a +3000 CE. Nessuna dipendenza da JPL/SPK.
> I due sistemi sono completamente separati. Il flag viene controllato all'ingresso in `swe_calc_ut()`/`swe_calc()`.

- [x] **TODO 1: Gate di routing in `swe_calc_ut()` e `swe_calc()` basato su `SEFLG_MOSEPH`** — `libephemeris/planets.py:736-840`. PROBLEMA: Attualmente `swe_calc_ut()` (linea 782) chiama `validate_jd_range()` e poi `_calc_body()` (Skyfield/JPL) per tutti i corpi in `_PLANET_MAP`, ignorando il flag `SEFLG_MOSEPH`. Non esiste un branch che instradi verso un calcolo Moshier alternativo. IMPATTO: Qualsiasi utente che passi `SEFLG_MOSEPH` ottiene silenziosamente risultati JPL (il flag viene ignorato), o un `EphemerisRangeError` per date fuori DE440, senza alcuna indicazione che Moshier non è implementato. FIX: All'inizio di `swe_calc_ut()` (dopo il check `SE_ECL_NUT`), aggiungere: `if iflag & SEFLG_MOSEPH: return _calc_body_moshier(t, ipl, iflag)`. Stesso pattern in `swe_calc()` (linea 824). La funzione `_calc_body_moshier()` è il nuovo dispatcher (TODO 3). Quando `SEFLG_MOSEPH` è attivo, NON chiamare `validate_jd_range()` (il range è diverso). Aggiungere un `validate_jd_range_moshier()` dedicato in `exceptions.py` con range JD 625673.5 (-3000) a 3182395.5 (+3000). RISULTATO ATTESO: `SEFLG_MOSEPH` instranda correttamente al sistema Moshier. `SEFLG_SWIEPH`/`SEFLG_JPLEPH` continuano a usare JPL con `EphemerisRangeError` fuori range. Nessun fallback implicito.

- [x] **TODO 2: Creare modulo `libephemeris/moshier/__init__.py` con struttura package** — Nuovo package `libephemeris/moshier/`. PROBLEMA: Non esiste alcun modulo per i calcoli semi-analitici. Tutto il codice astronomico in libephemeris dipende da Skyfield e file SPK. Serve un package separato che contenga gli algoritmi VSOP87, ELP 2000-82B e la teoria di Plutone, senza alcuna dipendenza da Skyfield/SPK. IMPATTO: Senza un package dedicato, il codice Moshier dovrebbe essere mescolato dentro `planets.py` (già ~5000 righe) creando un file ingestibile. FIX: Creare `libephemeris/moshier/` con: `__init__.py` (export pubblici), `vsop87.py` (pianeti), `elp82b.py` (Luna), `pluto.py` (Plutone), `precession.py` (precessione/nutazione IAU 2006), `utils.py` (coordinate eclittiche, conversioni, aberrazione). Ogni modulo espone una funzione `calc_position(jd_tt, body_id) -> (lon, lat, dist, speed_lon, speed_lat, speed_dist)`. RISULTATO ATTESO: Package `libephemeris.moshier` importabile, con sottommoduli per ogni teoria. Nessuna dipendenza da Skyfield. Solo numpy come dipendenza esterna.

- [x] **TODO 3: Implementare dispatcher `_calc_body_moshier()` in `libephemeris/planets.py`** — `libephemeris/planets.py` (nuova funzione, inserire dopo `_calc_body()` ~linea 1930). PROBLEMA: Serve un dispatcher analogo a `_calc_body()` che instradi i calcoli verso i moduli Moshier anziché Skyfield. Deve gestire gli stessi body ID (`SE_SUN`..`SE_PLUTO`, `SE_MEAN_NODE`, `SE_TRUE_NODE`, `SE_MEAN_APOG`, `SE_OSCU_APOG`) e gli stessi flag (SEFLG_SPEED, SEFLG_SIDEREAL, SEFLG_EQUATORIAL, SEFLG_HELCTR, SEFLG_TOPOCTR). IMPATTO: Senza questo dispatcher, il gate di routing (TODO 1) non ha dove instradare. FIX: Creare `_calc_body_moshier(t_or_jd, ipl, iflag)` che: (1) converte UT1→TT se necessario, (2) chiama `moshier.vsop87.calc_position()` per SE_SUN..SE_NEPTUNE, (3) chiama `moshier.elp82b.calc_position()` per SE_MOON, (4) chiama `moshier.pluto.calc_position()` per SE_PLUTO, (5) delega a `lunar.py` esistente per nodi/Lilith (già analitici, non dipendono da JPL), (6) applica correzioni sidereal/equatorial/topocentric riusando il codice già in `_calc_body()`. RISULTATO ATTESO: `_calc_body_moshier()` restituisce la stessa tupla `((lon,lat,dist,spd_lon,spd_lat,spd_dist), iflag)` di `_calc_body()`, con precisione <1 arcsec per pianeti, <5 arcsec per Luna.

- [x] **TODO 4: Implementare VSOP87 per i pianeti (Mercurio-Nettuno + Sole) in `libephemeris/moshier/vsop87.py`** — Nuovo file. PROBLEMA: Serve l'algoritmo VSOP87 (Bretagnon & Francou 1988) per calcolare le posizioni eliocentriche di Mercurio-Nettuno. Swiss Ephemeris usa la versione VSOP87 troncata di Moshier con ~2400 termini per pianeta (serie in longitudine, latitudine, raggio). La precisione è <1 arcsec per -3000..+3000 CE. IMPATTO: Senza VSOP87 non è possibile calcolare alcun pianeta in modalità Moshier. FIX: Implementare VSOP87 serie C (coordinate eliocentriche eclittiche): per ogni pianeta, calcolare L, B, R come somma di serie trigonometriche `Σ A·cos(B + C·t)` con t in millenni giuliani da J2000. Le tabelle dei coefficienti (~50KB per pianeta) vanno in `libephemeris/moshier/vsop87_data.py` come array numpy. Convertire da coordinate eliocentriche a geocentriche sottraendo la posizione della Terra. Calcolare velocità per differenza numerica (dt=1s). Per il Sole: posizione geocentrica = Terra + 180°. RISULTATO ATTESO: Posizioni eliocentriche dei pianeti entro 1 arcsec da DE440 per date 1550-2650 (intervallo di sovrapposizione). Posizioni calcolabili per -3000..+3000 senza file SPK.

- [x] **TODO 5: Implementare ELP 2000-82B per la Luna in `libephemeris/moshier/elp82b.py`** — Nuovo file. PROBLEMA: Serve la teoria lunare ELP 2000-82B (Chapront-Touzé & Chapront 1983) per calcolare la posizione geocentrica della Luna. Swiss Ephemeris usa la versione ridotta di Moshier con ~1600 termini (serie principali + perturbazioni planetarie). La precisione è ~0.5-5 arcsec per -3000..+3000 CE. IMPATTO: Senza ELP la Luna non è calcolabile in modalità Moshier. La Luna è il corpo più richiesto in astrologia. FIX: Implementare ELP serie: (1) argomenti fondamentali (Delaunay: l, l', F, D, Ω) come polinomi in T (secoli giuliani da J2000), (2) serie principale: longitudine (~470 termini), latitudine (~270 termini), distanza (~170 termini) come `Σ A·sin(combinazione lineare degli argomenti)`, (3) perturbazioni planetarie (~200 termini addizionali). Coefficienti in `libephemeris/moshier/elp82b_data.py`. Convertire in coordinate eclittiche. Velocità per differenza numerica. RISULTATO ATTESO: Posizione geocentrica della Luna entro 5 arcsec da DE440 nell'intervallo di sovrapposizione. Calcolabile per -3000..+3000.

- [x] **TODO 6: Implementare teoria analitica di Plutone in `libephemeris/moshier/pluto.py`** — Nuovo file. PROBLEMA: VSOP87 non include Plutone. Swiss Ephemeris usa una teoria analitica dedicata di Moshier basata su un fit polinomiale-trigonometrico alle effemeridi numeriche JPL DE404, con ~800 termini. Il range è ristretto a -3000..+3000 con precisione ~1-3 arcsec. IMPATTO: Senza questa teoria, Plutone (essenziale in astrologia) non è calcolabile in modalità Moshier. FIX: Implementare la teoria Plutone di Moshier: (1) coordinate eliocentriche J2000 calcolate come serie trigonometriche in 3 frequenze fondamentali più polinomi in T, (2) i coefficienti (~15KB) vanno in `libephemeris/moshier/pluto_data.py`, (3) applicare precessione da J2000 all'eclittica della data, (4) convertire da eliocentrico a geocentrico sottraendo la posizione della Terra (calcolata con VSOP87, TODO 4). RISULTATO ATTESO: Posizione eliocentrica di Plutone entro 3 arcsec da DE440 nell'intervallo di sovrapposizione. Calcolabile per -3000..+3000.

- [x] **TODO 7: Implementare precessione e nutazione standalone in `libephemeris/moshier/precession.py`** — Nuovo file. PROBLEMA: Il modulo Moshier deve poter calcolare precessione e nutazione senza dipendere da Skyfield (che usa i file IERS). Swiss Ephemeris Moshier usa: precessione IAU 2006 (Capitaine et al.), nutazione IAU 2000B (77 termini, sufficiente per <1 mas). Attualmente `erfa_nutation.py` ha già IAU 2000A/B via pyerfa, ma dipende da pyerfa che potrebbe non essere disponibile. IMPATTO: Senza precessione/nutazione standalone, le coordinate eclittiche della data (richieste da tutti i chart astrologici) non sono calcolabili in modalità Moshier. FIX: Implementare in puro numpy: (1) precessione IAU 2006: matrice P da J2000 all'eclittica/equatore della data usando i polinomi di Capitaine (ψ_A, ω_A, χ_A, etc.), (2) nutazione IAU 2000B: 77 termini trigonometrici per Δψ e Δε, (3) obliquità: polinomio IAU 2006 per ε₀ + Δε, (4) funzione `ecliptic_of_date(jd_tt, xyz_j2000) -> (lon, lat, dist)` che applica precessione + nutazione + conversione a eclittiche. Se pyerfa è disponibile, usarlo come acceleratore; altrimenti fallback a numpy puro. RISULTATO ATTESO: Precessione/nutazione calcolabile senza Skyfield né pyerfa. Precisione nutazione <1 mas. Nessuna dipendenza esterna oltre numpy.

- [x] **TODO 8: Implementare aberrazione e light-time standalone in `libephemeris/moshier/utils.py`** — Nuovo file. PROBLEMA: Le posizioni planetarie apparenti richiedono correzioni per aberrazione annuale (~20 arcsec) e light-time (fino a ~8 minuti per pianeti esterni). In modalità JPL queste correzioni sono gestite da Skyfield internamente. In modalità Moshier devono essere calcolate esplicitamente. Swiss Ephemeris Moshier usa: iterazione light-time (3 iterazioni), aberrazione stellare classica (Ron-Vondrák o Bradley). IMPATTO: Senza queste correzioni le posizioni hanno errori sistematici fino a 20 arcsec (aberrazione) + fino a 2° (light-time per Saturno). FIX: Implementare in `moshier/utils.py`: (1) `light_time_correction(jd_tt, earth_pos, target_func) -> corrected_pos`: iterare 3 volte `t_corrected = t - dist/c`, ricalcolando la posizione del target, (2) `annual_aberration(earth_velocity, target_dir) -> correction`: formula classica `Δα = -κ·(cos(α)·cos(λ☉)·cos(ε) + sin(α)·sin(λ☉)) / cos(δ)` semplificata con vettori, dove κ = 20.49552 arcsec, (3) conversione rettangolari ↔ sferiche, eclittiche ↔ equatoriali. RISULTATO ATTESO: Posizioni apparenti corrette per light-time e aberrazione. Errore residuo <0.01 arcsec rispetto a Skyfield.

- [x] **TODO 9: Gestire corpi non supportati in modalità Moshier (asteroidi, TNO, stelle fisse)** — `libephemeris/planets.py` in `_calc_body_moshier()`. PROBLEMA: In modalità Moshier, gli asteroidi (Chiron SE_CHIRON=15, Ceres SE_CERES=17, Pallas SE_PALLAS=18, Juno SE_JUNO=19, Vesta SE_VESTA=20), i TNO e le stelle fisse NON sono calcolabili perché richiedono kernel SPK o cataloghi stellari. Swiss Ephemeris in modalità Moshier restituisce un errore per questi corpi. IMPATTO: Se un utente chiede Chiron con `SEFLG_MOSEPH`, il sistema deve dare un errore chiaro, non risultati vuoti o crash. Kerykeion usa Chiron di default nei chart, quindi deve gestire la sua assenza. FIX: In `_calc_body_moshier()`, per body ID non supportati (asteroidi, TNO, stelle fisse, pianeti ipotetici), sollevare `CalculationError("Body {name} (ID {ipl}) is not available in Moshier ephemeris mode. Use SEFLG_SWIEPH with a JPL ephemeris file for this body.")`. Definire `_MOSHIER_SUPPORTED_BODIES = {SE_SUN, SE_MOON, SE_MERCURY, ..., SE_PLUTO, SE_MEAN_NODE, SE_TRUE_NODE, SE_MEAN_APOG, SE_OSCU_APOG}`. RISULTATO ATTESO: Errore esplicito e documentato per corpi non supportati. Nessun crash o risultato silenziosamente errato.

- [ ] **TODO 10: Aggiungere test di confronto JPL vs Moshier in `compare_scripts/tests/test_moshier/`** — Nuova directory. PROBLEMA: Non esistono test che verifichino la precisione del sistema Moshier rispetto a JPL. Serve una suite di test che confronti le posizioni di tutti i corpi supportati calcolate con `SEFLG_MOSEPH` vs `SEFLG_SWIEPH` nell'intervallo di sovrapposizione (1550-2650), e che verifichi il funzionamento per date fuori range DE440. IMPATTO: Senza test di confronto, non possiamo verificare la precisione dell'implementazione né prevenire regressioni. FIX: Creare: (1) `test_moshier_vs_jpl.py`: per 10 date campione (1550, 1700, 1900, 2000, 2024, 2100, 2300, 2450, 2600, 2650), calcolare tutti i pianeti con entrambi i flag e verificare che la differenza sia <1 arcsec per pianeti, <5 arcsec per Luna, <3 arcsec per Plutone. (2) `test_moshier_range.py`: verificare che date -3000, -1000, 0, 500, 1000, 1549, 2651, 3000 funzionino con `SEFLG_MOSEPH` e sollevino `EphemerisRangeError` con `SEFLG_SWIEPH`. (3) `test_moshier_unsupported.py`: verificare che Chiron, Ceres, asteroidi sollevino `CalculationError` con `SEFLG_MOSEPH`. RISULTATO ATTESO: Suite di 50+ test che validano precisione, range e gestione errori del sistema Moshier.

- [ ] **TODO 11: Aggiornare `constants.py` e `__init__.py` per documentare `SEFLG_MOSEPH` come supportato** — `libephemeris/constants.py:529`, `libephemeris/__init__.py`. PROBLEMA: `SEFLG_MOSEPH` è definito a linea 529 come `# Use Moshier ephemeris (not supported)`. Il commento e la documentazione indicano che il flag non è implementato. L'`__init__.py` non esporta nessuna API pubblica per il sistema Moshier. IMPATTO: Gli utenti non sanno che Moshier è disponibile e non hanno documentazione su limiti e precisione. FIX: (1) Aggiornare commento a linea 529: `# Use Moshier semi-analytical ephemeris (VSOP87 + ELP2000-82B, range -3000..+3000 CE)`. (2) Aggiornare commento del blocco a linea 526: rimuovere "currently only SWIEPH/JPL mode supported". (3) In `__init__.py`, aggiungere `SEFLG_MOSEPH` e `FLG_MOSEPH` alla lista `__all__` se non già presenti. (4) Aggiungere docstring a `swe_calc_ut()` che documenti il comportamento con i tre flag ephemeris. RISULTATO ATTESO: Documentazione accurata. `SEFLG_MOSEPH` visibile nell'API pubblica. Utenti sanno range, precisione e limitazioni (no asteroidi).

- [ ] **TODO 12: Supportare `swe_houses()` e `swe_houses_ex()` in modalità Moshier** — `libephemeris/houses.py:690-1560`. PROBLEMA: Le funzioni houses (`swe_houses`, `swe_houses_ex`) chiamano internamente `swe_calc_ut()` per ottenere la posizione del Sole (necessaria per Sunshine houses, linea 751) e `swe_get_ayanamsa_ut()` per il modo siderale. Queste chiamate ereditano il flag ephemeris dal contesto ma non lo propagano esplicitamente. Se l'utente usa `SEFLG_MOSEPH` per i pianeti ma `swe_houses()` internamente chiama JPL, c'è inconsistenza. IMPATTO: Cuspidi houses calcolate con JPL anche quando l'utente ha richiesto Moshier. Per date fuori DE440 range, `swe_houses()` fallisce con `EphemerisRangeError` anche con Moshier attivo. FIX: Aggiungere parametro opzionale `iflag=0` a `swe_houses()` (linea 690) e `swe_houses_ex()` (linea 1405). Propagare il flag alle chiamate interne `swe_calc_ut(tjdut, SE_SUN, SEFLG_EQUATORIAL | iflag)` a linea 751. Per `swe_houses_armc()` (linea 1200), l'ARMC è già fornito dall'utente quindi non serve il flag. RISULTATO ATTESO: Houses calcolate coerentemente con il sistema ephemeris scelto dall'utente. Houses calcolabili per date -3000..+3000 con `SEFLG_MOSEPH`.

## PRECISION

- [ ] [Creare modulo logging centralizzato `libephemeris/logging_config.py` con logger dedicato e handler stderr]. PROBLEMA: libephemeris non usa il modulo `logging` di Python. Tutti i download (DE440 ~114MB, SPK asteroidi 1-10MB) avvengono silenziosamente senza alcun messaggio su stdout/stderr. I test pytest non mostrano alcuna indicazione di attività durante operazioni di rete che possono durare 10-120 secondi. IMPATTO: I test sembrano bloccati senza spiegazione. Gli utenti non sanno se il programma sta funzionando o è in stallo. Impossibile debuggare problemi di rete o timeout. CI/CD fallisce per timeout senza diagnostica utile. UX pessima per primo avvio che richiede download silenzioso di ~150MB. FIX: Creare `libephemeris/logging_config.py` con: `logger = logging.getLogger("libephemeris")`, handler StreamHandler(stderr), formatter `[libephemeris] %(levelname)s: %(message)s`, livello default INFO per download e WARNING per altri. Esportare `get_logger()` per uso in altri moduli. RISULTATO ATTESO: Tutti i download mostrano messaggi tipo `[libephemeris] INFO: Downloading DE440 ephemeris (114 MB)...` visibili in pytest output, console, e log file. Tempo di diagnosi problemi ridotto da ore a secondi.

- [ ] [Aggiungere logging a `get_planets()` in `libephemeris/state.py:121-165` per download DE440]. PROBLEMA: La funzione `get_planets()` scarica DE440 (~114MB) alla prima chiamata quando l'efemeride non è in cache. Il download avviene dentro `load(_EPHEMERIS_FILE)` di Skyfield (linea 161) senza alcun messaggio. Durante i test questo causa blocchi apparenti di 30-120 secondi senza output. IMPATTO: Primo avvio di qualsiasi applicazione che usa libephemeris sembra bloccato. Test CI timeout senza spiegazione. Utenti pensano che l'installazione sia corrotta. Impossibile distinguere tra download lento e bug reale. IMPATTO: Supporto tecnico sommerso da segnalazioni "il programma non parte". FIX: Prima di `load()` linea 161: `logger.info(f"Downloading JPL ephemeris {_EPHEMERIS_FILE}...")`. Dopo load riuscito: `logger.info(f"Ephemeris loaded: {bsp_path}")`. Se file esiste già: `logger.debug(f"Using cached ephemeris: {bsp_path}")`. RISULTATO ATTESO: Output visibile `[libephemeris] INFO: Downloading JPL ephemeris de440.bsp...` seguito da `[libephemeris] INFO: Ephemeris loaded: /path/de440.bsp`. Utenti e CI sanno esattamente cosa sta succedendo.

- [ ] [Aggiungere logging a `download_spk()` in `libephemeris/spk.py:180-280` per download kernel asteroidi]. PROBLEMA: La funzione `download_spk()` scarica kernel SPK da JPL Horizons API senza alcun messaggio. Ogni download richiede 5-60 secondi a seconda della dimensione e velocità rete. Per 5 asteroidi (Chiron, Ceres, Pallas, Juno, Vesta) il tempo totale può essere 1-5 minuti di silenzio apparente. IMPATTO: Test che usano asteroidi sembrano bloccati. Impossibile sapere quale asteroide sta causando ritardo. Nessuna indicazione di progresso per download grandi. Utenti interrompono pensando sia bug. Debug di problemi rete impossibile. FIX: Aggiungere prima di urllib request: `logger.info(f"Downloading SPK for {body_name} (NAIF {naif_id}) from JPL Horizons...")`. Dopo download completato: `logger.info(f"SPK saved: {cache_path} ({size_mb:.1f} MB)")`. In caso di errore rete: `logger.warning(f"SPK download failed for {body_name}: {error}")`. RISULTATO ATTESO: Output tipo `[libephemeris] INFO: Downloading SPK for Chiron (NAIF 2002060)...` visibile durante ogni download SPK. Diagnosi immediata di quale corpo causa ritardo.

- [ ] [Aggiungere logging a `ensure_major_asteroid_spk()` in `libephemeris/minor_bodies.py:850-920` per pre-download asteroidi]. PROBLEMA: La funzione `ensure_major_asteroid_spk()` è il punto di ingresso raccomandato per pre-scaricare SPK, ma non produce output. Quando chiamata in loop per 5 asteroidi, l'utente vede solo silenzio per minuti. Il check `is_spk_available_for_body()` interno non indica se sta usando cache o scaricando. IMPATTO: Fixture pytest che pre-scaricano SPK sembrano bloccate. Impossibile verificare se cache funziona o sta ri-scaricando. Nessun feedback su quali asteroidi sono già in cache. Spreco banda se ri-download non necessari non vengono rilevati. FIX: All'inizio: `logger.debug(f"Checking SPK availability for body {body_id}")`. Se già in cache: `logger.debug(f"SPK for {body_id} already cached")`. Se download necessario: `logger.info(f"SPK for {body_name} not cached, downloading...")`. RISULTATO ATTESO: Distinzione chiara tra cache hit (DEBUG) e download effettivo (INFO). Utenti vedono `[libephemeris] INFO: SPK for Chiron not cached, downloading...` solo quando serve.

- [ ] [Modificare `_calc_body()` in `libephemeris/planets.py:1400-1500` per auto-download SPK quando flag SWIEPH/JPLEPH attivo]. PROBLEMA: Quando `strict_precision=True` e SPK non disponibile per un asteroide, `_calc_body()` solleva `SPKRequiredError` invece di tentare download automatico. L'utente deve chiamare manualmente `ensure_major_asteroid_spk()` prima di ogni calcolo. Questo rompe il principio "just works" e causa 5 aspetti mancanti nei test kerykeion (tutti quelli con Chiron). IMPATTO: Test falliscono con errore criptico invece di scaricare automaticamente. Ogni progetto downstream deve implementare logica di pre-download. UX pessima: errore invece di azione correttiva. 100+ test kerykeion falliscono per mancanza Chiron SPK. FIX: In `_calc_body()` per corpi in `MAJOR_ASTEROID_SPK_INFO`, prima di sollevare `SPKRequiredError`: check `if get_auto_spk_download(): logger.info(f"Auto-downloading SPK for {body_name}..."); if ensure_major_asteroid_spk(ipl, tjd): return _calc_minor_body_spk(...)`. Solo se auto-download fallisce, solleva errore. RISULTATO ATTESO: Con `set_auto_spk_download(True)`, calcoli asteroidi funzionano automaticamente scaricando SPK on-demand. Log visibile durante download. Zero errori SPKRequired per asteroidi principali.

- [ ] [Aggiungere env var `LIBEPHEMERIS_AUTO_SPK=1` in `libephemeris/state.py:36-50` per abilitare auto-download via ambiente]. PROBLEMA: L'auto-download SPK richiede chiamata Python `set_auto_spk_download(True)`. Non esiste modo di abilitarlo via environment variable. Questo rende impossibile configurare il comportamento in CI/CD senza modificare codice test. Docker containers e GitHub Actions non possono abilitare auto-download senza custom entrypoint. IMPATTO: Ogni progetto deve modificare conftest.py per abilitare SPK. CI/CD richiede codice boilerplate. Impossibile testare con/senza SPK via env var. Configurazione non portabile tra ambienti. FIX: In `state.py` dopo imports, leggere env: `_AUTO_SPK_DOWNLOAD = os.environ.get("LIBEPHEMERIS_AUTO_SPK", "0") == "1"`. Modificare `get_auto_spk_download()` per usare questa variabile come default. Documentare in docstring di `set_auto_spk_download()`. RISULTATO ATTESO: `LIBEPHEMERIS_AUTO_SPK=1 pytest` abilita auto-download senza modifiche codice. CI può configurare via env. Default resta disabilitato per backward compatibility.

- [ ] [Aggiungere env var `LIBEPHEMERIS_LOG_LEVEL` in `libephemeris/logging_config.py` per controllo verbosità]. PROBLEMA: Non esiste modo di controllare il livello di logging di libephemeris. In produzione si vuole WARNING, in debug si vuole DEBUG, nei test INFO. Senza env var bisogna modificare codice per cambiare verbosità. Logging troppo verboso in produzione, troppo silenzioso in debug. IMPATTO: Impossibile debuggare problemi senza modificare codice. Log produzione inquinati o troppo silenziosi. Nessun controllo granulare per CI vs development. Utenti non possono aumentare verbosità per diagnosi. FIX: In `logging_config.py`: `level = os.environ.get("LIBEPHEMERIS_LOG_LEVEL", "WARNING").upper(); logger.setLevel(getattr(logging, level, logging.WARNING))`. Valori validi: DEBUG, INFO, WARNING, ERROR. Default WARNING per non spammare in produzione. RISULTATO ATTESO: `LIBEPHEMERIS_LOG_LEVEL=DEBUG pytest -s` mostra tutti i messaggi. `LIBEPHEMERIS_LOG_LEVEL=INFO` mostra solo download. Produzione silenziosa di default. Configurabile senza modifiche codice.

- [ ] [Aggiungere progress bar per download >1MB in `libephemeris/spk.py` usando `SimpleProgressBar` da `download.py`]. PROBLEMA: Download di file grandi (DE440 114MB, SPK grandi fino a 10MB) non mostrano progresso. L'utente vede solo messaggio iniziale e poi silenzio per 30-120 secondi. Impossibile distinguere download lento da download bloccato. Nessuna stima tempo rimanente. IMPATTO: UX pessima per download lunghi. Utenti interrompono pensando sia bloccato. Nessuna indicazione se connessione lenta o file grande. Impossibile stimare tempo completamento. Debug problemi rete difficile. FIX: Importare `SimpleProgressBar` da `download.py`. Per download >1MB e stderr is tty: creare progress bar con total=content_length. Aggiornare durante read chunks. Formato: `[libephemeris] Downloading: [████████----] 67% (7.2/10.8 MB)`. Skip progress bar se stderr non è terminale (pipe/file). RISULTATO ATTESO: Download grandi mostrano progress bar animata. Utenti vedono progresso reale. Stima tempo basata su velocità download. UX professionale paragonabile a pip/npm.

- [ ] [Documentare fixture pytest per progetti downstream in `libephemeris/README.md` o nuovo file `docs/testing.md`]. PROBLEMA: Progetti che usano libephemeris (kerykeion, altri) non hanno documentazione su come configurare SPK auto-download nei loro test. Ogni progetto deve scoprire da solo le API `set_auto_spk_download()`, `ensure_major_asteroid_spk()`. Nessun esempio di fixture session-scoped per pre-download. IMPATTO: Ogni progetto downstream reimplementa stessa logica. Errori comuni non documentati. Tempo setup test aumentato. Best practices non condivise. Supporto richieste ripetute per stessa configurazione. FIX: Creare sezione "Testing with SPK" in README o doc dedicato. Includere esempio completo di fixture: `@pytest.fixture(scope="session", autouse=True) def setup_spk(): eph.set_auto_spk_download(True); for body in [SE_CHIRON,...]: eph.ensure_major_asteroid_spk(body); yield`. Spiegare env vars e logging. RISULTATO ATTESO: Progetti downstream copiano fixture documentata. Zero domande su configurazione SPK per test. Best practice codificata. Tempo setup test ridotto da ore a minuti.

- [ ] [Aggiungere costante `REQUIRED_SPK_BODIES` in `libephemeris/constants.py` con lista corpi che richiedono SPK per precisione]. PROBLEMA: Non esiste lista centralizzata dei corpi che richiedono SPK per calcoli precisi. `MAJOR_ASTEROID_SPK_INFO` in `minor_bodies.py` contiene mapping ma non è esportato pubblicamente. Utenti devono indovinare quali corpi necessitano SPK. Documentazione sparsa e incompleta. IMPATTO: Utenti non sanno quali asteroidi pre-scaricare. Progetti scaricano troppi o troppo pochi SPK. Nessuna API per iterare corpi SPK-required. Codice downstream duplica liste hardcoded. FIX: In `constants.py` aggiungere: `REQUIRED_SPK_BODIES = {SE_CHIRON, SE_CERES, SE_PALLAS, SE_JUNO, SE_VESTA, SE_PHOLUS, SE_NESSUS, SE_ERIS}` con commento che spiega perché. Esportare in `__init__.py`. Usare in `ensure_major_asteroid_spk()`. RISULTATO ATTESO: `for body in eph.REQUIRED_SPK_BODIES: eph.ensure_major_asteroid_spk(body)` scarica tutti gli SPK necessari. Lista mantenuta centralmente. Downstream usa API pubblica.
