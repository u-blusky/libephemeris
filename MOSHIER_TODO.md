# MOSHIER TODO - Test di comparazione pyswisseph vs libephemeris con SEFLG_MOSEPH

- [x] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_planets.py`: file analogo a `compare_scripts/tests/test_compare_planets.py` (1015 righe, classi TestPlanetaryPositions/TestInnerPlanets/TestOuterPlanets/TestEdgeCases) ma con `SEFLG_MOSEPH` al posto di `SEFLG_SWIEPH`; comparare `swe.calc_ut(jd, ipl, swe.FLG_MOSEPH)` vs `ephem.swe_calc_ut(jd, ipl, SEFLG_MOSEPH)` per tutti i 10 pianeti (SE_SUN..SE_PLUTO) su TEST_DATES identiche a quelle usate in test_compare_planets.py (J2000.0, 2024, 1900, 2100, 1950) [`libephemeris/planets.py:1087-1155`, `libephemeris/moshier/__init__.py:1-287`]. PROBLEMA: Il file `test_compare_planets.py` confronta `swe.calc_ut()` vs `ephem.swe_calc_ut()` con `SEFLG_SWIEPH` (JPL) e ha stabilito tolleranze precise (LONGITUDE_STRICT=0.001 deg, LATITUDE_STRICT=0.001 deg, DISTANCE_STRICT=0.0001 AU, VELOCITY_ANGULAR=0.01 deg/day); non esiste l'equivalente con `SEFLG_MOSEPH` per verificare che il path Moshier della C library e quello Python producano gli stessi risultati; i test esistenti in `test_moshier/test_moshier_vs_jpl.py` confrontano solo libephemeris Moshier vs libephemeris JPL (test interno, non cross-library). IMPATTO: Senza il mirror Moshier di test_compare_planets.py, divergenze sistematiche tra la reimplementazione VSOP87/ELP Python e l'originale C di Steve Moshier integrata in Swiss Ephemeris restano invisibili; l'intera suite compare_scripts copre JPL ma non Moshier, creando un buco nella validazione cross-library; eventuali errori di frame (mean ecliptic vs J2000 ICRS) nel Moshier Python non vengono rilevati dal confronto con la C library. FIX: Copiare la struttura esatta di `test_compare_planets.py` (stesse classi TestPlanetaryPositions, TestInnerPlanets, TestOuterPlanets con `@pytest.mark.comparison`, stessa `angular_diff()` locale, stessi PLANETS e TEST_DATES); sostituire `SEFLG_SWIEPH` con `swe.FLG_MOSEPH`/`SEFLG_MOSEPH`; definire tolleranze Moshier-specifiche come costanti modulo: MOSHIER_LONGITUDE=0.01 deg, MOSHIER_LATITUDE=0.01 deg, MOSHIER_DISTANCE_REL=0.001, MOSHIER_VELOCITY=0.01 deg/day; parametrizzare con `@pytest.mark.parametrize("planet_id,planet_name", PLANETS)` e `@pytest.mark.parametrize("year,month,day,hour,date_desc", TEST_DATES)`. RISULTATO ATTESO: 50 test case geocentrici (10 pianeti x 5 date) con assertion su lon/lat/dist passano con tolleranze Moshier, replicando la copertura di test_compare_planets.py per la modalita Moshier.

- [x] Aggiungere in `test_moshier_compare_planets.py` la classe `TestMoshierInnerPlanets` con test dedicati a Sun, Moon, Mercury, Venus, Mars con `swe.FLG_MOSEPH | swe.FLG_SPEED`, analogo a `TestInnerPlanets` in `test_compare_planets.py` che verifica posizioni e velocita dei pianeti interni [`libephemeris/planets.py:1087-1155`, `libephemeris/moshier/elp82b.py:1-581`, `libephemeris/moshier/vsop87.py:1-1138`]. PROBLEMA: I pianeti interni (specialmente Moon e Mercury) hanno velocita angolari elevate (Moon ~13 deg/day, Mercury fino a 2 deg/day) e le differenze numeriche nella differenziazione delle velocita tra C e Python sono amplificate; il test_compare_planets.py ha TestInnerPlanets con 5 inner planets x 5 dates x SEFLG_SPEED che verifica tutti i 6 componenti; la Moon usa ELP 2000-82B che ha 60+ termini periodici con coefficienti che possono differire leggermente tra le implementazioni. IMPATTO: La Luna e il corpo con la velocita piu alta e il piu critico per calendari, eclissi e astrologia; Mercury ha le retrogradazioni piu frequenti (3-4 volte/anno) e errori di velocita causano timing sbagliato delle stazioni; senza test dedicati inner planets Moshier, queste divergenze critiche restano invisibili. FIX: Creare `TestMoshierInnerPlanets` con test `test_inner_planet_speed_longitude` e `test_inner_planet_speed_latitude` parametrizzati su INNER_PLANETS x TEST_DATES con `swe.FLG_MOSEPH | swe.FLG_SPEED`; asserire speed_lon < 0.01 deg/day, speed_lat < 0.001 deg/day, speed_dist < 0.001 AU/day; per Moon usare tolleranze rilassate speed_lon < 0.05 deg/day dato che 13 deg/day amplifica le differenze numeriche. RISULTATO ATTESO: 25 test case (5 inner x 5 dates) validano le velocita Moshier dei pianeti interni, con Moon a 0.05 deg/day e Mercury-Mars a 0.01 deg/day.

- [x] Aggiungere in `test_moshier_compare_planets.py` la classe `TestMoshierOuterPlanets` con test per Jupiter, Saturn, Uranus, Neptune, Pluto con `swe.FLG_MOSEPH`, analogo a `TestOuterPlanets` in `test_compare_planets.py` [`libephemeris/moshier/vsop87.py:1-1138`, `libephemeris/moshier/pluto.py:1-425`]. PROBLEMA: I pianeti esterni hanno precisione VSOP87 decrescente: Jupiter/Saturn sono accurati a sub-arcsecondo, Uranus/Neptune degradano leggermente, Pluto usa la teoria Chapront-Francou completamente diversa da VSOP87 con tabelle di perturbazione in `pluto_data.py` (2993 righe); il test_compare_planets.py ha TestOuterPlanets che usa tolleranze rilassate per heliocentric/barycentric ma non testa Moshier; Pluto in test_moshier_vs_jpl.py ha tolleranza 60000 arcsec che e troppo larga per un confronto C vs Python dello stesso algoritmo. IMPATTO: Pluto e il pianeta dove la differenza C-Python e piu probabile a causa delle tabelle numeriche; Uranus/Neptune sono generazionali e errori posizionali impattano analisi di ere e cicli lunghi; senza confronto outer planets C-Python, non si distingue tra errore del porting e errore intrinseco della teoria. FIX: Creare `TestMoshierOuterPlanets` parametrizzato su OUTER_PLANETS x TEST_DATES; per Jupiter-Neptune usare MOSHIER_LONGITUDE=0.01 deg; per Pluto creare tolleranza separata PLUTO_LONGITUDE=0.05 deg (molto piu stretta dei 60000 arcsec attuali, perche qui confrontiamo Moshier-C vs Moshier-Python, non Moshier vs JPL); asserire anche distanza e latitudine. RISULTATO ATTESO: 25 test case (5 outer x 5 dates) validano outer planets Moshier, con Pluto a 0.05 deg confermando la qualita del porting della teoria Chapront-Francou.

- [x] Aggiungere in `test_moshier_compare_planets.py` la classe `TestMoshierICRSPositions` con flag `SEFLG_MOSEPH | SEFLG_ICRS`, analogo a `TestICRSPositions` in `test_compare_planets.py:390-440` che testa il frame ICRS [`libephemeris/planets.py:857-861`]. PROBLEMA: `test_compare_planets.py` ha `TestICRSPositions` e `TestJ2000Positions` che testano `SEFLG_ICRS` e `SEFLG_J2000` con `SEFLG_SWIEPH`; libephemeris in modalita Moshier potrebbe non gestire correttamente `SEFLG_ICRS` perche le posizioni VSOP87 sono in eclittica media, non ICRS; il flag potrebbe essere silenziosamente ignorato o produrre risultati errati; la C library applica una frame rotation matrix. IMPATTO: ICRS e il frame standard internazionale per astronomia (IERS/IAU); applicazioni astronomiche che usano `SEFLG_ICRS` con Moshier otterranno risultati diversi tra C e Python; se il flag e ignorato in Python ma applicato in C, le posizioni differiranno di ~50 mas (frame tie rotation). FIX: Creare `TestMoshierICRSPositions` con 5 pianeti x 3 date x `swe.FLG_MOSEPH | swe.FLG_ICRS`, tolleranza < 0.01 deg; creare anche `TestMoshierJ2000Positions` con `SEFLG_J2000` e `TestMoshierNoNutation` con `SEFLG_NONUT`; documentare se la C library e Python gestiscono questi flag diversamente in Moshier. RISULTATO ATTESO: 45 test case (3 classi x 5 pianeti x 3 date) documentano il supporto di ICRS/J2000/NONUT in Moshier, con xfail se non implementati e tolleranze appropriate se implementati.

- [x] Aggiungere in `test_moshier_compare_planets.py` la classe `TestMoshierEdgeCases` con date estreme e casi limite, analogo a `TestEdgeCases` in `test_compare_planets.py` [`libephemeris/planets.py:774-1084`]. PROBLEMA: `test_compare_planets.py` ha `TestEdgeCases` che testa date ai confini del range DE440 (1550, 2650); per Moshier i confini sono diversi (~-3000 a +3000 CE via libephemeris, ~-13200 a +17200 via C library); nessun test verifica il comportamento alle date estreme Moshier ne la continuita posizionale attraverso il millennio; le teorie VSOP87/ELP degradano per date lontane da J2000 e il tasso di degradazione puo differire tra C e Python. IMPATTO: Le date estreme sono il caso d'uso primario di Moshier (fuori DE440); archeologia astronomica per datazione eventi antichi, astro-cronologia e simulazione di civilta antiche dipendono dalla precisione Moshier a date estreme; errori non rilevati alle estremita del range producono datazioni sbagliate. FIX: Creare `TestMoshierEdgeCases` con date: (-2000, 1, 1, 12.0), (-1000, 6, 15, 12.0), (0, 1, 1, 12.0), (500, 3, 21, 12.0), (1000, 12, 25, 12.0), (1200, 7, 4, 12.0), (2700, 1, 1, 12.0), (3000, 6, 15, 12.0); testare Sun, Moon, Mars, Jupiter, Saturn con tolleranze graduali: < 0.01 deg per 1000-1550 CE, < 0.1 deg per 0-1000 CE, < 0.5 deg per -1000-0 CE, < 1.0 deg per < -1000 CE. RISULTATO ATTESO: 40 test case (8 date x 5 pianeti) con report di degradazione per epoca, quantificando se le differenze C-Python crescono con la distanza da J2000.

- [x] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_houses.py`: file analogo a `compare_scripts/tests/test_compare_houses.py` (273 righe, 24 house systems, TestHouseCusps/TestAscendantMC/TestRandomLocations/TestHighLatitudes/TestSpecialCases) ma con `SEFLG_MOSEPH`; comparare `swe.houses_ex(jd, lat, lon, hsys.encode("ascii"), swe.FLG_MOSEPH)` vs `ephem.swe_houses_ex(jd, lat, lon, hsys, SEFLG_MOSEPH)` [`libephemeris/houses.py:522-577`, `libephemeris/houses.py:762-765`, `libephemeris/houses.py:1474-1549`]. PROBLEMA: `test_compare_houses.py` compara tutte le 24 house systems tra swe e ephem con HOUSE_CUSP_TOL=0.001 deg e ASCMC_TOL=0.001 deg per `SEFLG_SWIEPH`; nessun file equivalente esiste per `SEFLG_MOSEPH`; in houses.py il flag Moshier e propagato via `eph_flags = iflag & (SEFLG_JPLEPH | SEFLG_SWIEPH | SEFLG_MOSEPH)` alla linea 765 e usato in `swe_calc_ut` per il calcolo del Sole (Sunshine houses I/i); la sidereal time (ARMC) e l'obliquita (eps) sono calcolate da Skyfield in libephemeris ma dal Moshier interno nella C library, producendo discrepanze strutturali nelle cuspidi. IMPATTO: Le cuspidi delle case sono il componente piu visibile di un oroscopo; `test_compare_houses.py` e il file piu importante della suite compare_scripts per la validazione astrologica; senza l'equivalente Moshier, la migrazione da pyswisseph a libephemeris per date fuori DE440 e completamente non validata; errori nelle cuspidi propagano a house_pos, Gauquelin sectors, e a tutta l'interpretazione astrologica. FIX: Copiare la struttura di `test_compare_houses.py`: stesse HOUSE_SYSTEMS (24 sistemi), STANDARD_LOCATIONS (Roma, New York, Sydney, Equator), RELAXED_SYSTEMS dict, stessa `angular_diff()` locale; sostituire flag con `swe.FLG_MOSEPH`/`SEFLG_MOSEPH`; gestire pyswisseph `swe.houses(jd, lat, lon, hsys.encode("ascii"))` con flag Moshier vs `ephem.swe_houses(jd, lat, lon, hsys, SEFLG_MOSEPH)`; usare MOSHIER_CUSP_TOL=0.01 deg (piu rilassato di 0.001 perche ARMC/eps divergono). RISULTATO ATTESO: 96 test case (24 sistemi x 4 locations) + test AscMC + test high latitudes passano con tolleranze Moshier, validando tutte le 24 house systems per date nel range Moshier.

- [x] Aggiungere in `test_moshier_compare_houses.py` le classi `TestMoshierRandomLocations` e `TestMoshierHighLatitudes` con 50 localita random e 10 localita polari, analogo a `TestRandomLocations` e `TestHighLatitudes` in `test_compare_houses.py` [`libephemeris/houses.py:522-577`]. PROBLEMA: `test_compare_houses.py` ha `TestRandomLocations` con 50 localita random (seed=42) e `TestHighLatitudes` con localita a 65-89.9 gradi di latitudine; le case a latitudini polari sono problematiche per sistemi come Placidus e Koch che richiedono l'attraversamento del meridiano; in Moshier, l'obliquita e l'ARMC calcolati diversamente possono spostare il threshold di latitudine polare, causando fallback a sistemi alternativi in casi diversi tra C e Python. IMPATTO: Le latitudini polari sono edge case critici: Tromso (69.6N), Reykjavik (64.1N), Murmansk (68.9N) sono citta con significativa popolazione astrologica; differenze nel threshold polare significano che per la stessa citta, una implementazione usa Placidus e l'altra fallback a Porphyry, producendo oroscopi completamente diversi. FIX: Creare `TestMoshierRandomLocations` con `random_locations(50, seed=42)` x 5 house systems (P, K, E, W, O) con `SEFLG_MOSEPH`; `TestMoshierHighLatitudes` con POLAR_LOCATIONS (65N, 67N, 70N, 80N, 85N, 89.9N, e simmetriche S) x 3 sistemi (E, W, O che non hanno problemi polari); tolleranza < 0.02 deg. RISULTATO ATTESO: 250 test case random + 42 test polari validano le case Moshier su distribuzione geografica completa, documentando eventuali fallback polari.

- [x] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_houses_ex2.py`: comparare `swe.houses_ex2(jd, lat, lon, hsys, swe.FLG_MOSEPH | swe.FLG_SPEED)` vs `ephem.swe_houses_ex2(jd, lat, lon, hsys, SEFLG_MOSEPH | SEFLG_SPEED)` per le velocita delle cuspidi, seguendo il pattern delle houses con tolleranza velocita [`libephemeris/houses.py:1579-1700`]. PROBLEMA: `swe_houses_ex2` restituisce 4 tuple (cusps, ascmc, cusps_speed, ascmc_speed) quando `SEFLG_SPEED` e impostato; le velocita delle cuspidi in Moshier sono calcolate tramite differenziazione numerica in libephemeris ma con metodo integrato nella C library; nessun test confronta le velocita delle cuspidi tra le due implementazioni in modalita Moshier; la struttura del ritorno potrebbe differire (pyswisseph cusps 1-indexed vs libephemeris 0-indexed). IMPATTO: Le velocita delle cuspidi sono usate per primary directions, solar arc directions e progressed houses; divergenze compromettono il timing delle direzioni primarie per date storiche; `houses_ex2` e l'API piu avanzata per case e la mancanza di validazione Moshier la rende inaffidabile per utenti professionali. FIX: Parametrizzare su 5 house systems (P, K, E, W, O) x 3 locations x 3 date con `swe.FLG_MOSEPH | swe.FLG_SPEED`; confrontare cusps_speed con tolleranza < 0.1 deg/day e ascmc_speed con tolleranza < 0.05 deg/day; verificare struttura ritorno identica (4 tuple); testare anche senza SEFLG_SPEED per verificare velocita zero. RISULTATO ATTESO: 45 test case con velocita + 45 senza velocita confermano che houses_ex2 Moshier produce struttura e valori identici a pyswisseph.

- [x] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_lunar.py`: file analogo a `compare_scripts/tests/test_compare_lunar.py` (280 righe, TestMeanNode/TestTrueNode/TestMeanLilith/TestTrueLilith/TestNodalMotion/TestLilithLatitude) ma con `SEFLG_MOSEPH`; comparare tutti i 4 punti lunari [`libephemeris/planets.py:871-943`, `libephemeris/lunar.py`]. PROBLEMA: `test_compare_lunar.py` confronta swe vs ephem per SE_MEAN_NODE(10), SE_TRUE_NODE(11), SE_MEAN_APOG(12), SE_OSCU_APOG(13) con tolleranze MEAN_NODE_TOL=0.01 deg, TRUE_NODE_TOL=0.15 deg, MEAN_LILITH_TOL=0.01 deg, TRUE_LILITH_TOL=0.1 deg usando `SEFLG_SWIEPH`; nessun equivalente esiste per `SEFLG_MOSEPH`; in Moshier, i nodi lunari sono calcolati da `lunar.py` (polinomi di Meeus per Mean Node, perturbazioni analitiche per True Node) mentre la C library ha la propria implementazione; la Lilith media usa `calc_mean_lilith_with_latitude()` e la Lilith osculante `calc_true_lilith()`. IMPATTO: I nodi lunari (Rahu/Ketu) sono i punti piu importanti in Jyotish dopo i graha; Mean Lilith e usata in astrologia moderna; True Node determina le eclissi; differenze nei punti lunari Moshier rendono impossibile la migrazione per astrologia vedica con date storiche. FIX: Copiare struttura di `test_compare_lunar.py` con stesse classi e fixture `test_dates` dal conftest.py; sostituire flag con `swe.FLG_MOSEPH`/`SEFLG_MOSEPH`; usare tolleranze Moshier rilassate: MEAN_NODE_MOSHIER=0.02 deg, TRUE_NODE_MOSHIER=0.2 deg, MEAN_LILITH_MOSHIER=0.02 deg, TRUE_LILITH_MOSHIER=0.2 deg; aggiungere test velocita nodali con `SEFLG_SPEED`. RISULTATO ATTESO: 36 test case (4 punti x 9 date) + 8 test velocita validano i punti lunari Moshier cross-library.

- [x] Aggiungere in `test_moshier_compare_lunar.py` la classe `TestMoshierNodalMotion` con verifica del moto retrogrado dei nodi e `TestMoshierLilithLatitude` per la latitudine di Lilith, analoghi a `TestNodalMotion` e `TestLilithLatitude` in `test_compare_lunar.py` [`libephemeris/lunar.py`, `libephemeris/planets.py:871-928`]. PROBLEMA: `test_compare_lunar.py` ha `TestNodalMotion` che verifica il moto retrogrado del Mean Node (~-0.053 deg/day) e `TestLilithLatitude` che verifica che la latitudine di Mean Lilith sia non-zero e ragionevole; in Moshier Python, il moto nodale e calcolato con differenziazione numerica (dt=0.5 in planets.py:876-884) che potrebbe dare velocita leggermente diverse dalla C library; la latitudine di Mean Lilith usa `calc_mean_lilith_with_latitude()` che implementa la formula di Chapront diversamente dalla C. IMPATTO: Il moto retrogrado dei nodi determina quando i nodi cambiano segno (ciclo 18.6 anni); errori nella velocita nodale spostano il timing dei transiti nodali; la latitudine di Lilith influenza gli aspetti con l'eclittica e la sua visibilita nella carta. FIX: Creare `TestMoshierNodalMotion` che verifica speed_lon < 0 per Mean Node e |speed_lon| tra 0.04-0.07 deg/day in entrambe le implementazioni; `TestMoshierLilithLatitude` che verifica |lat| < 6 deg per Mean Lilith e confronta lat tra C e Python con tolleranza < 0.1 deg. RISULTATO ATTESO: 18 test case validano proprieta qualitative (retrogradazione, range latitudine) dei punti lunari Moshier in entrambe le implementazioni.

- [x] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_sidereal.py`: file analogo a `compare_scripts/tests/test_compare_sidereal.py` (501 righe, TestAyanamshaValues/TestSiderealPlanets/TestMajorAyanamshas/TestAyanamshaProgression/TestJ2000Mode/TestUserDefinedAyanamsha) ma con `SEFLG_MOSEPH`; comparare ayanamsha e posizioni siderali [`libephemeris/planets.py:1071-1082`, `libephemeris/planets.py:3403-3460`, `libephemeris/planets.py:2446-2471`]. PROBLEMA: `test_compare_sidereal.py` testa 43 ayanamsha modes (28 formula-based + 15 star-based) con STRICT_TOLERANCE=0.001 deg per formula-based e RELAXED_TOLERANCE=0.1 deg per star-based; verifica che `swe.set_sid_mode(mode)` + `swe.get_ayanamsa_ut(jd)` corrisponda a `ephem.swe_set_sid_mode(mode)` + `ephem.swe_get_ayanamsa_ut(jd)` con `SEFLG_SWIEPH`; nessun test verifica la catena siderale con `SEFLG_MOSEPH`; in Moshier, `swe_get_ayanamsa_ex_ut(jd, SEFLG_MOSEPH)` dovrebbe usare posizioni Moshier per ayanamsha star-based (True Citra, True Revati) ma le stelle fisse non sono disponibili in Moshier. IMPATTO: L'intero ecosistema Jyotish dipende da ayanamsha accurate; `test_compare_sidereal.py` e il file di riferimento per la validazione Jyotish; senza l'equivalente Moshier, le posizioni siderali per date storiche sono completamente non validate; 43 ayanamsha modes non testati per Moshier e un gap critico. FIX: Copiare struttura di `test_compare_sidereal.py`; per `TestMoshierAyanamshaValues` testare 28 formula-based ayanamsha con `swe.get_ayanamsa_ut(jd)` vs `ephem.swe_get_ayanamsa_ut(jd)` (entrambi dopo set_sid_mode) per 5 date nel range Moshier; per star-based, testare `get_ayanamsa_ex_ut(jd, swe.FLG_MOSEPH)` e documentare il comportamento; per `TestMoshierSiderealPlanets` testare 5 pianeti x 5 ayanamsha x `SEFLG_MOSEPH | SEFLG_SIDEREAL`; tolleranza formula-based < 0.001 deg, star-based < 0.1 deg. RISULTATO ATTESO: 140 test case (28 ayanamsha x 5 date) + 125 test siderali (5 pianeti x 5 ayanamsha x 5 date) validano l'intero ecosistema siderale Moshier.

- [x] Aggiungere in `test_moshier_compare_sidereal.py` la classe `TestMoshierUserDefinedAyanamsha` con SE_SIDM_USER, analogo a `TestUserDefinedAyanamsha` in `test_compare_sidereal.py` che testa `swe.set_sid_mode(swe.SIDM_USER, t0, ayan_t0)` con parametri personalizzati [`libephemeris/planets.py:3403-3460`]. PROBLEMA: `test_compare_sidereal.py` ha `TestUserDefinedAyanamsha` che verifica la definizione custom di ayanamsha con t0 e ayan_t0; in Moshier, l'ayanamsha user-defined potrebbe comportarsi diversamente perche la precessione usata per estrapolre l'ayanamsha da t0 alla data target differisce tra la IAU 2006 di libephemeris e la precessione della C library; con date storiche lontane da t0, questa differenza di precessione si amplifica. IMPATTO: Ayanamsha user-defined sono usati da scuole astrologiche specifiche (es. KP, Raman personalizzato); errori nell'estrapolazione producono spostamenti progressivi delle posizioni siderali per date lontane da t0, invalidando sistemi di previsione personalizzati. FIX: Testare `set_sid_mode(SE_SIDM_USER, 2451545.0, 23.5)` in entrambe le implementazioni per 3 date con `SEFLG_MOSEPH`, confrontare ayanamsha con tolleranza < 0.01 deg; verificare anche con t0 storici (es. 1900) per amplificare differenze di precessione. RISULTATO ATTESO: 6 test case validano la compatibilita ayanamsha user-defined Moshier con estrapolazione corretta.

- [x] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_coordinates.py`: file analogo a `compare_scripts/tests/test_compare_coordinates.py` (472 righe, TestCotrans/TestAzalt/TestAzaltRev/TestRefrac) ma usando input derivati da calcoli Moshier; comparare `cotrans()`, `azalt()`, `azalt_rev()`, `refrac()` con posizioni calcolate via `SEFLG_MOSEPH` [`libephemeris/utils.py`, `libephemeris/planets.py:1052-1069`]. PROBLEMA: `test_compare_coordinates.py` compara le funzioni di trasformazione coordinate (eclittica-equatoriale, equatoriale-orizzontale, rifrazione) tra swe e ephem usando posizioni JPL; nessun test usa posizioni derivate da Moshier come input; le trasformazioni coordinate stesse non dipendono dall'efferide, ma l'obliquita usata in Moshier (IAU 2006 da `moshier.mean_obliquity()`) potrebbe differire dall'obliquita usata nella C library (Lieske 1979 o Owen 1990); se le posizioni di input (Moshier) e l'obliquita differiscono, le coordinate trasformate divergono. IMPATTO: Le trasformazioni coordinate sono la base di rise/set, azimut, altitudine e visibilita; errori nell'obliquita Moshier si propagano a cotrans (ecliptic->equatorial), poi a azalt (equatorial->horizontal), producendo azimut e altezze errate per applicazioni osservative che usano Moshier per date storiche. FIX: Per ogni test in test_compare_coordinates.py, creare la variante Moshier: calcolare posizioni Sun/Moon con `SEFLG_MOSEPH | SEFLG_EQUATORIAL` da entrambe le implementazioni; usare queste come input per cotrans, azalt, azalt_rev; confrontare output con tolleranze: angoli < 0.01 deg, rifrazione < 0.01 deg; testare anche cotrans_sp con velocita Moshier. RISULTATO ATTESO: 48 test case validano la catena completa posizione Moshier -> trasformazione coordinate -> orizzontali.

- [x] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_time.py`: file analogo a `compare_scripts/tests/test_compare_time.py` (709 righe, TestDeltat/TestSiderealTime) ma focalizzato su Delta T e sidereal time con `SEFLG_MOSEPH`; comparare `swe.deltat_ex(jd, swe.FLG_MOSEPH)` vs `ephem.swe_deltat_ex(jd, SEFLG_MOSEPH)` per date nel range Moshier esteso [`libephemeris/time_utils.py:201-269`]. PROBLEMA: `test_compare_time.py` ha TestDeltat con DELTA_T_TOL=0.2 secondi per `SEFLG_SWIEPH`; libephemeris usa Skyfield per Delta T (tabelle IERS/Espenak-Meeus) mentre la C library usa Morrison-Stephenson per date storiche; per date prima del 1620 la differenza tra i due modelli puo superare 30 secondi; `swe_deltat_ex` con `SEFLG_MOSEPH` in libephemeris ignora il flag Moshier per Delta T (ritorna lo stesso valore di SEFLG_SWIEPH, come documentato in time_utils.py:269); la C library potrebbe invece usare un modello Delta T specifico per Moshier. IMPATTO: Delta T influenza tutte le posizioni via la conversione UT1->TT; una differenza di 10 secondi in Delta T produce ~5 arcsec nella longitudine lunare; per date antiche (anno 0) le differenze possono essere enormi (~7200 secondi) invalidando tutte le posizioni Moshier calcolate; `test_compare_time.py` e il file di riferimento per la validazione temporale. FIX: Copiare la sezione TestDeltat di test_compare_time.py; parametrizzare su 20 date dal -2999 al +3000 CE; confrontare Delta T in secondi con tolleranze graduali per epoca: < 0.5s per 1900-2100, < 5s per 1620-1900, < 30s per 1000-1620, < 120s per 0-1000, < 600s per < 0 CE; aggiungere test sidtime con `swe.sidtime(jd)` vs `ephem.sidtime(jd)` per date Moshier. RISULTATO ATTESO: 20 test case Delta T + 10 test sidereal time producono un report dettagliato delle divergenze temporali per epoca.

- [x] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_phenomena.py`: file analogo a `compare_scripts/tests/test_compare_phenomena.py` (326 righe, TestPhenoUt/TestPheno/TestInnerPlanetElongations/TestOuterPlanetOppositions) ma con `swe.FLG_MOSEPH`; comparare `swe.pheno_ut(jd, planet, swe.FLG_MOSEPH)` vs `ephem.swe_pheno_ut(jd, planet, SEFLG_MOSEPH)` [`libephemeris/planets.py:4781-4822`]. PROBLEMA: `test_compare_phenomena.py` ha PhenoTolerance (ANGLE_DEGREES=0.01, MAGNITUDE=0.1, DIAMETER=0.01) per `SEFLG_SWIEPH`; pheno_ut calcola fase, elongazione, diametro apparente, magnitudine e elongazione disco; con Moshier, sia le posizioni del pianeta che del Sole sono Moshier, amplificando potenzialmente le differenze; la magnitudine dipende dalla distanza che puo differire tra C e Python; nessun test confronta pheno_ut in modalita Moshier. IMPATTO: Magnitudine e elongazione sono critiche per visibilita heliacal, pianificazione osservativa e determinazione stella mattutina/serale; errori nella magnitudine Moshier invalidano calcoli di visibilita per date storiche (eclissi antiche, apparizioni di comete). FIX: Copiare struttura di test_compare_phenomena.py; testare Mercury, Venus, Mars, Jupiter, Saturn con `swe.FLG_MOSEPH` per 3 date; confrontare i 5 componenti con tolleranze rilassate per Moshier: ANGLE=0.1 deg, MAGNITUDE=0.5, DIAMETER=0.1 arcsec; includere TestMoshierElongations per elongazioni massime di Mercury e Venus. RISULTATO ATTESO: 15 test case fenomeni + 6 test elongazioni validano pheno_ut Moshier cross-library.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_orbital.py`: file analogo a `compare_scripts/tests/test_compare_orbital.py` (285 righe, TestNodAps/TestOrbitalElements/TestOrbitalDistance) ma con `SEFLG_MOSEPH`; comparare `swe.nod_aps_ut(jd, planet, swe.FLG_MOSEPH, method)` vs `ephem.swe_nod_aps_ut(jd, planet, SEFLG_MOSEPH, method)` [`libephemeris/planets.py:3584-3633`, `libephemeris/planets.py:3981-4036`]. PROBLEMA: `test_compare_orbital.py` ha TestNodAps con xfail (helio vs geo methodology difference) e TestOrbitalElements con OrbitalTolerance (ANGLE_DEGREES=0.01, DISTANCE_AU=0.001, ECCENTRICITY=0.0001, PERIOD_DAYS=0.1) per `SEFLG_SWIEPH`; nod_aps_ut con Moshier chiama internamente swe_calc_ut con `SEFLG_MOSEPH`; orbital_elements con Moshier potrebbe non essere supportato (dipende dal path interno); nessun test confronta queste funzioni orbitali in modalita Moshier. IMPATTO: Nodi planetari (nodo nord di Plutone, nodo di Saturno) sono usati in astrologia evolutiva; elementi orbitali sono usati per calcolare eccentricita, inclinazione e semiasse maggiore; la mancanza di validazione Moshier rende queste funzioni inutilizzabili per date storiche. FIX: Copiare struttura di test_compare_orbital.py; per nod_aps_ut testare Mars, Jupiter, Saturn con SE_NODBIT_MEAN e SE_NODBIT_OSCU x 3 date con `SEFLG_MOSEPH`; per orbital_elements testare `swe.get_orbital_elements(jd, planet, swe.FLG_MOSEPH)` vs ephem equivalente; usare xfail per funzioni non supportate in Moshier; tolleranza angoli < 0.1 deg. RISULTATO ATTESO: 18 test nod_aps + 15 test orbital elements (con xfail appropriati) documentano lo stato di supporto orbitale Moshier.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_observations.py`: file analogo a `compare_scripts/tests/test_compare_observations.py` (294 righe, TestObservationModes/TestTopocentric/TestHeliocentric/TestJ2000Coordinates) ma con `SEFLG_MOSEPH` per tutte le modalita osservative [`libephemeris/planets.py:948-1069`]. PROBLEMA: `test_compare_observations.py` testa topocentric (con `swe.set_topo()`), heliocentric (`SEFLG_HELCTR`), e J2000 (`SEFLG_J2000`) con dati di nascita di personaggi famosi (Einstein, Gandhi, Mandela); in Moshier, topocentric e marcato `pass # TODO` alla riga 1049 di planets.py, heliocentric usa dispatch separato (vsop87_heliocentric, pluto_heliocentric), J2000 potrebbe non essere gestito; nessun test confronta queste modalita osservative con Moshier. IMPATTO: Topocentric e essenziale per parallasse lunare (~1 deg); heliocentric per cosmobiologia; J2000 per astronomia; senza validazione Moshier di queste modalita, libephemeris non puo sostituire pyswisseph per nessuna applicazione osservativa con date storiche; il `pass # TODO` per topocentric e una bomba a orologeria per la migrazione. FIX: Copiare struttura di test_compare_observations.py con SUBJECTS (famous people birth data); per `TestMoshierTopocentric` usare `@pytest.mark.xfail(reason="Moshier topocentric not implemented (planets.py:1049)")` e impostare set_topo prima di calc_ut con `SEFLG_MOSEPH | SEFLG_TOPOCTR`; per `TestMoshierHeliocentric` testare 8 pianeti x 3 date con `SEFLG_MOSEPH | SEFLG_HELCTR`; per `TestMoshierJ2000` testare con `SEFLG_MOSEPH | SEFLG_J2000`. RISULTATO ATTESO: Test topocentric xfail documentano il gap; 24 test heliocentric + 15 test J2000 validano le modalita implementate.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_calc_pctr.py`: file analogo a `compare_scripts/tests/test_compare_calc_pctr.py` (669 righe, TestMoonFromMars/TestSunFromJupiter/TestKeyCombinations/TestAllPlanetCenters) ma con `SEFLG_MOSEPH`; comparare `swe.calc_pctr(jd, target, center, swe.FLG_MOSEPH)` vs `ephem.swe_calc_pctr(jd, target, center, SEFLG_MOSEPH)` [`libephemeris/planets.py:1221-1300`]. PROBLEMA: `test_compare_calc_pctr.py` confronta le posizioni planetocentriche (es. Moon vista da Mars, Sun vista da Jupiter) con LONGITUDE_TOLERANCE=0.02 deg e LATITUDE_TOLERANCE=0.02 deg per `SEFLG_SWIEPH`; `swe_calc_pctr` con Moshier chiama internamente `swe_calc_ut` due volte (per target e center) con `SEFLG_MOSEPH`; le differenze posizionali di base si sommano nel calcolo planetocentrico; pyswisseph potrebbe non supportare calc_pctr con Moshier. IMPATTO: calc_pctr e usato per aspetti planetocentrici in cosmobiologia avanzata e per simulazioni del sistema solare da prospettive diverse; errori cumulativi (target + center) nel Moshier amplificano le divergenze; la mancanza di validazione rende calc_pctr Moshier inaffidabile. FIX: Testare le stesse combinazioni chiave di test_compare_calc_pctr.py (Moon da Mars, Sun da Jupiter, Venus da Saturn) x 3 date con `SEFLG_MOSEPH`; usare try/except con pytest.skip se calc_pctr non supporta Moshier in pyswisseph; tolleranza MOSHIER_PCTR=0.1 deg (piu rilassata perche errori si sommano). RISULTATO ATTESO: 9 test case (3 combinazioni x 3 date) validano calc_pctr Moshier o documentano la non-disponibilita.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_heliocentric.py`: test dedicato alle coordinate eliocentriche Moshier per tutti i 8 pianeti + Earth + Pluto con `SEFLG_MOSEPH | SEFLG_HELCTR`, seguendo il pattern di TestHeliocentric in `test_compare_observations.py` e TestMoshierHeliocentricMode in `tests/test_calc_body_moshier.py` [`libephemeris/planets.py:948-1025`, `libephemeris/moshier/vsop87.py:calc_vsop87_heliocentric`, `libephemeris/moshier/pluto.py:calc_pluto_heliocentric`]. PROBLEMA: Le coordinate eliocentriche Moshier hanno un dispatch completamente separato in libephemeris: `calc_vsop87_heliocentric()` per Mercury-Neptune, `calc_pluto_heliocentric()` per Pluto, `calc_earth_heliocentric()` per Earth/Moon; il Sun ritorna (0,0,0); test_calc_body_moshier.py verifica Sun=origin e Mars=valid ma solo internamente; la C library ha un unico codepath per heliocentric Moshier che potrebbe differire; le velocita eliocentriche usano step numerico h=0.001 per pianeti e h=0.01 per Pluto. IMPATTO: Le posizioni eliocentriche sono la base per aspetti eliocentrici (scuola Hamburg/Uranian), geocentric->heliocentric conversion, e calcoli orbitali; Earth eliocentrica e cruciale perche il suo errore propaga a tutte le posizioni geocentriche via la formula geo = helio_planet - helio_earth; Moon eliocentrica approssimata come Earth e un'approssimazione che la C library potrebbe non fare. FIX: Parametrizzare su HELIO_BODIES (Mercury, Venus, Earth, Mars, Jupiter, Saturn, Uranus, Neptune, Pluto) x 5 date x `swe.FLG_MOSEPH | swe.FLG_HELCTR | swe.FLG_SPEED`; aggiungere test speciale per Sun heliocentrico (0,0,0); aggiungere test Moon heliocentrica con confronto C vs Python; tolleranza lon < 0.01 deg, dist < 0.1%, velocita < 0.01 deg/day. RISULTATO ATTESO: 50 test case eliocentrici validano l'intero dispatch heliocentric Moshier inclusi i casi speciali Sun, Moon, Earth, Pluto.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_equatorial.py`: test dedicato alle coordinate equatoriali RA/Dec Moshier con `SEFLG_MOSEPH | SEFLG_EQUATORIAL`, verificando la catena obliquita->trasformazione->RA/Dec tra pyswisseph e libephemeris [`libephemeris/planets.py:1052-1069`, `libephemeris/moshier/precession.py:1-826`]. PROBLEMA: La conversione eclittica-equatoriale in libephemeris Moshier usa `moshier.mean_obliquity()` (IAU 2006, testata in test_moshier_precession.py con ~23.44 deg a J2000) e `moshier.ecliptic_to_equatorial(lon, lat, obliquity)`; le velocita equatoriali usano differenziazione numerica con step 0.001; la C library usa la propria obliquita interna (potenzialmente Lieske 1979) e formule di trasformazione potenzialmente diverse; test_compare_planets.py non testa equatorial con Moshier; la differenza di obliquita tra IAU 2006 e Lieske 1979 e ~0.01 arcsec ma si accumula per date lontane da J2000. IMPATTO: RA/Dec sono le coordinate fondamentali per osservazione telescopica, cataloghi stellari, e puntamento strumenti; errori in RA/Dec Moshier rendono libephemeris inutilizzabile per osservazioni con date storiche; l'errore si propaga a azalt (azimut/altitudine), heliacal_ut (visibilita), e rise_trans (alba/tramonto). FIX: Parametrizzare su 8 pianeti (Sun-Pluto escluso Earth) x 5 date x `swe.FLG_MOSEPH | swe.FLG_EQUATORIAL | swe.FLG_SPEED`; confrontare RA con angular_diff (wrap 0/360), Dec con abs diff; tolleranza RA < 0.01 deg (36 arcsec), Dec < 0.01 deg, speed_RA < 0.01 deg/day; aggiungere test con `SEFLG_NONUT` per isolare effetto nutazione. RISULTATO ATTESO: 40 test case equatoriali + 16 test NONUT validano la catena trasformazione Moshier, quantificando l'impatto della diversa obliquita.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_sidereal_houses.py`: comparare case siderali con `SEFLG_MOSEPH | SEFLG_SIDEREAL` per i 3 ayanamsha principali (Lahiri, Fagan-Bradley, Raman) x 7 house systems x 4 locations, seguendo il pattern combinato di test_compare_houses.py e test_compare_sidereal.py [`libephemeris/houses.py:1474-1549`]. PROBLEMA: La combinazione Moshier + Sidereal nelle houses richiede: (1) calcolo posizioni con Moshier, (2) calcolo ARMC/eps, (3) calcolo cuspidi, (4) sottrazione ayanamsha; ogni step ha una potenziale divergenza C-Python; `test_compare_houses.py` non testa sidereal, `test_compare_sidereal.py` non testa houses; la combinazione MOSEPH+SIDEREAL nelle houses e completamente non validata cross-library; per Whole Sign, le cuspidi dipendono dal segno dell'Ascendente siderale, quindi un errore di 0.5 deg nell'Asc puo spostare tutte le cuspidi di 30 deg. IMPATTO: L'astrologia Jyotish usa quasi esclusivamente Whole Sign siderale con Lahiri; se la combinazione MOSEPH+SIDEREAL produce Ascendenti in segni diversi tra C e Python, l'intero oroscopo vedico e sbagliato; questo e il test piu critico per la comunita Jyotish che usa date storiche. FIX: Per 3 ayanamsha x 7 house systems (P, K, E, W, O, R, C) x 4 locations x 3 date, impostare sid_mode in entrambe le implementazioni, chiamare houses_ex con `SEFLG_MOSEPH | SEFLG_SIDEREAL`; confrontare cuspidi con tolleranza < 0.05 deg; per Whole Sign verificare che le cuspidi siano multiple di 30 deg e che il segno dell'Asc sia identico. RISULTATO ATTESO: 252 test case (3 x 7 x 4 x 3) validano la combinazione MOSEPH+SIDEREAL per tutte le permutazioni astrologicamente rilevanti.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_deltat.py`: test dedicato al confronto Delta T con `SEFLG_MOSEPH` per date sparse nel range Moshier, seguendo il pattern di `TestDeltat` in `test_compare_time.py` [`libephemeris/time_utils.py:201-269`]. PROBLEMA: `test_compare_time.py` ha TestDeltat con tolleranza 0.2 secondi per date moderne con `SEFLG_SWIEPH`; in `time_utils.py:266-269` e documentato che `SEFLG_MOSEPH` usa lo stesso modello Delta T di Skyfield (non un modello Moshier specifico); la C library potrebbe usare un modello Delta T diverso per il path Moshier; la funzione `swe_deltat_ex` accetta `SEFLG_MOSEPH` e il suo secondo ritorno (warning string) potrebbe differire; per date storiche (anno 0, -1000) la differenza tra modelli Delta T (Skyfield vs Espenak-Meeus vs Morrison-Stephenson) puo superare centinaia di secondi. IMPATTO: Delta T e il cuore di tutte le conversioni temporali UT1->TT; ogni secondo di differenza produce ~0.5 arcsec nella longitudine lunare; per date antiche, le differenze di Delta T dominano l'errore totale delle posizioni Moshier; questa e l'informazione piu critica per determinare se le differenze posizionali C-Python sono dovute a Delta T o alle teorie VSOP87/ELP. FIX: Parametrizzare su 25 date distribuite: 5 moderne (1900-2100), 5 pre-moderne (1600-1900), 5 medievali (1000-1600), 5 antiche (0-1000), 5 storiche (-2999-0); chiamare `swe.deltat_ex(jd, swe.FLG_MOSEPH)` e `ephem.swe_deltat_ex(jd, SEFLG_MOSEPH)`; confrontare Delta T in secondi; riportare differenza assoluta e percentuale; asserire tolleranze graduali. RISULTATO ATTESO: 25 test case producono un report completo Delta T C-vs-Python per 5 epoche, quantificando la divergenza del modello temporale come base per interpretare tutte le altre differenze posizionali.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_calc_tt.py`: comparare `swe.calc(jd_tt, planet, swe.FLG_MOSEPH)` vs `ephem.swe_calc(jd_tt, planet, SEFLG_MOSEPH)` per il path TT (Terrestrial Time), complementare a test_moshier_compare_planets.py che usa swe_calc_ut (UT1) [`libephemeris/planets.py:1170-1206`]. PROBLEMA: `swe_calc` accetta TT e libephemeris gestisce la conversione inversa `jd_ut = jd_tt - delta_t` in `_calc_body_moshier(is_ut=False)` alla riga 849-852 di planets.py; la C library potrebbe non fare questa conversione inversa o usare una formula diversa; per date storiche dove Delta T e grande (>7000 secondi prima del 1600), l'errore nella stima inversa si amplifica; test_compare_planets.py non testa swe_calc (solo swe_calc_ut); il test test_moshier_routing.py linee 323-332 verifica solo che swe_calc funzioni, non che corrisponda a pyswisseph. IMPATTO: Applicazioni astronomiche e scientifiche preferiscono TT a UT1; swe_calc e l'interfaccia nativa per TT; errori nel path TT Moshier invalidano effemeridi astronomiche precise, specialmente per date storiche dove la conversione inversa TT->UT e critica. FIX: Parametrizzare su 5 pianeti (Sun, Moon, Mars, Jupiter, Saturn) x 5 date in TT (incluse date con Delta T grande: anno 1000, 0 CE, -1000 CE); confrontare tutti i 6 componenti con stesse tolleranze del path UT1; aggiungere test di consistenza: swe_calc_ut(jd_ut) ~= swe_calc(jd_ut + deltat) per entrambe le implementazioni. RISULTATO ATTESO: 25 test case + 10 test consistenza validano il path TT Moshier cross-library.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_distance.py`: test dedicato al componente distanza (pos[2]) e velocita distanza (pos[5]) per tutti i corpi Moshier, verificando la costante UA e il centro di riferimento [`libephemeris/moshier/__init__.py:200-250`, `libephemeris/moshier/utils.py:AU_KM`]. PROBLEMA: La distanza in AU dipende dalla costante astronomica `AU_KM = 149597870.7` in moshier/utils.py; la C library potrebbe usare un valore leggermente diverso; la distanza geocentrica vs baricentrica potrebbe differire se le due implementazioni usano centri di riferimento diversi; Mean Node e Mean Lilith restituiscono distanza=0.0 in libephemeris (planets.py:874, 917) ma la C library potrebbe restituire un valore non-zero (es. la distanza media Terra-Luna per Lilith); Oscu Apog e True Node hanno distanze non-zero calcolate dal modello di perturbazione. IMPATTO: La distanza e usata per magnitudine apparente, diametro apparente, parallasse, e conversioni AU->km; un errore dello 0.01% nella distanza produce 0.0001 mag di errore nella magnitudine; la distanza della Luna (~0.0026 AU) e critica per eclissi e occultazioni; distanze zero per Mean Node/Lilith vs non-zero causano bug in applicazioni che calcolano aspetti fisici dei punti lunari. FIX: Parametrizzare su tutti 14 corpi Moshier (10 pianeti + 4 punti lunari) x 5 date con `swe.FLG_MOSEPH | swe.FLG_SPEED`; confrontare distanza con tolleranza relativa < 0.1% per pianeti, < 0.5% per Moon, assoluta < 0.0001 AU per punti lunari; confrontare speed_dist con tolleranza < 0.001 AU/day; documentare valori distanza per Mean Node/Mean Lilith in entrambe le implementazioni. RISULTATO ATTESO: 70 test case (14 corpi x 5 date) validano le distanze Moshier e documentano differenze nei punti lunari.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_ayanamsa_ex.py`: comparare `swe.get_ayanamsa_ex_ut(jd, swe.FLG_MOSEPH)` vs `ephem.swe_get_ayanamsa_ex_ut(jd, SEFLG_MOSEPH)` per tutti i 43 ayanamsha modes, estendendo il test_moshier_compare_sidereal.py con il path _ex specifico per il flag efferide [`libephemeris/planets.py:3498-3530`]. PROBLEMA: `swe_get_ayanamsa_ex_ut` prende un flag efferide che in teoria dovrebbe influenzare il calcolo dell'ayanamsha (per star-based ayanamsha, la posizione della stella fissa richiede un'efferide); con `SEFLG_MOSEPH`, le stelle fisse non sono disponibili in Moshier; la C library potrebbe fallback silenziosamente a JPL per star-based ayanamsha con Moshier, o lanciare errore, o restituire un valore approssimato; libephemeris potrebbe comportarsi diversamente; la differenza nel trattamento star-based con Moshier non e mai stata documentata. IMPATTO: I 15 star-based ayanamsha (True Citra, True Revati, True Pushya, True Mula, True Sheoran, e galactic center modes) sono usati da milioni di astrologi; se il comportamento con Moshier differisce tra C e Python, le posizioni siderali saranno inconsistenti; la documentazione di questo comportamento e critica per la migrazione. FIX: Per tutti i 43 ayanamsha modes x 3 date, chiamare get_ayanamsa_ex_ut con `swe.FLG_MOSEPH` in entrambe; per formula-based (28 modes), asserire tolleranza < 0.001 deg; per star-based (15 modes), documentare il comportamento: entrambe usano JPL fallback? errore? valore approssimato?; usare try/except per catturare comportamenti diversi. RISULTATO ATTESO: 129 test case (43 x 3) documentano il comportamento di get_ayanamsa_ex_ut Moshier per ogni singolo ayanamsha mode.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_houses_armc.py`: comparare `swe.houses_armc(armc, lat, eps, hsys)` vs `ephem.swe_houses_armc(armc, lat, eps, hsys)` usando valori ARMC/eps derivati da calcoli Moshier, per isolare differenze geometriche dalle differenze di efferide [`libephemeris/houses.py:1093-1380`]. PROBLEMA: `swe_houses_armc` e la funzione piu pura per testare le case perche prende ARMC, latitudine, obliquita e sistema come input, bypassando il calcolo dell'efferide; se dato lo stesso ARMC/eps/lat le cuspidi differiscono, l'errore e nell'algoritmo geometrico delle case, non nell'efferide; `test_compare_houses.py` testa `swe.houses()` che calcola ARMC internamente; nessun test usa `swe_houses_armc` per isolare l'algoritmo geometrico in confronto cross-library. IMPATTO: Se swe_houses_armc produce risultati diversi tra C e Python con gli stessi input, significa che l'algoritmo di calcolo delle case ha un bug fondamentale (non attribuibile a Moshier); questo e il diagnostic test piu importante per debugging di discrepanze nelle cuspidi; il test isola definitivamente il problema. FIX: Calcolare ARMC e eps dalla C library con `swe.calc_ut(jd, 0, swe.FLG_MOSEPH)` per 3 date; poi usare gli stessi valori ARMC/eps in `swe.houses_armc()` e `ephem.swe_houses_armc()` per 7 house systems x 5 latitudini (0, 30, 45, 60, 75); confrontare cuspidi con HOUSE_CUSP_TOL=0.001 deg (stessa tolleranza di test_compare_houses.py); aggiungere `swe_houses_armc_ex2` per velocita. RISULTATO ATTESO: 105 test case (7 x 5 x 3) confermano la correttezza geometrica delle case con stessi input, isolando differenze di algoritmo da differenze di efferide.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_house_pos.py`: comparare `swe.house_pos(armc, lat, eps, hsys, lon, lat_body)` vs `ephem.swe_house_pos(armc, lat, eps, hsys, lon, lat_body)` usando posizioni Moshier come input [`libephemeris/houses.py:4502-4870`]. PROBLEMA: `house_pos` determina in quale casa cade un pianeta dato ARMC/eps/posizione; i suoi input devono essere derivati da calcoli Moshier per date storiche; nessun test confronta house_pos cross-library con input Moshier; la funzione e overloaded con 4 firme diverse in libephemeris (houses.py:4502-4545) e la firma pyswisseph potrebbe differire; un errore in house_pos significa che il pianeta viene assegnato alla casa sbagliata, che e il bug piu visibile in un software astrologico. IMPATTO: house_pos e chiamata per ogni pianeta in un oroscopo (10 pianeti x 12 case = punto critico); se house_pos produce numeri di casa diversi tra C e Python per la stessa posizione Moshier, l'intero oroscopo e inconsistente; errori nell'interpolazione all'interno della casa influenzano aspect grids e tabelle di dignita. FIX: Per 3 date, calcolare ARMC/eps/posizioni di 5 pianeti con `SEFLG_MOSEPH` dalla C library; usare questi valori come input per house_pos in entrambe le implementazioni con 4 house systems (P, K, E, W); confrontare la posizione di casa (valore 1.0-13.0) con tolleranza < 0.05; verificare che floor(pos) sia identico (stessa casa). RISULTATO ATTESO: 60 test case (3 x 5 x 4) confermano che i pianeti vengono assegnati alla stessa casa da entrambe le implementazioni con input Moshier.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_gauquelin.py`: comparare `swe.gauquelin_sector()` vs `ephem.swe_gauquelin_sector()` con `SEFLG_MOSEPH`, seguendo il pattern dei test Gauquelin in `test_compare_houses.py` [`libephemeris/houses.py:4876-4930`]. PROBLEMA: I settori Gauquelin (36 settori basati sull'angolo orario) sono testati in test_compare_houses.py con GAUQUELIN_TOL=0.001 per `SEFLG_SWIEPH`; `gauquelin_sector` con `SEFLG_MOSEPH` richiede che il flag sia propagato a `swe_calc_ut` interno per ottenere la posizione del pianeta; in test_houses_moshier.py (linea 291) c'e un test che verifica i 36 settori ma solo internamente; nessun confronto cross-library. IMPATTO: La ricerca statistica Gauquelin (effetto Mars) e la base scientifica piu citata in astrologia; replicare studi Gauquelin con libephemeris per date storiche richiede settori identici a pyswisseph; discrepanze nel settore assegnato invalidano l'intera analisi statistica. FIX: Testare Mars, Jupiter, Saturn x 3 locations (Roma, Londra, New York da conftest.py test_locations) x 2 metodi (0=Gauquelin, 1=Kundig) x 2 date con `swe.FLG_MOSEPH`; confrontare settore con tolleranza < 0.5 (meno di mezzo settore); verificare range 1-36. RISULTATO ATTESO: 36 test case validano i settori Gauquelin Moshier cross-library.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_south_nodes.py`: comparare il comportamento dei nodi sud lunari (body ID negativi: -10, -11) con `SEFLG_MOSEPH` tra pyswisseph e libephemeris, documentando la compatibilita API [`libephemeris/planets.py:901-912`]. PROBLEMA: libephemeris supporta body ID negativi in Moshier: -SE_MEAN_NODE e -SE_TRUE_NODE ritornano nodo_nord+180 deg con latitudine negata (planets.py:902-912); pyswisseph potrebbe non supportare ID negativi in Moshier (lanciando SwissephError) o potrebbe implementarli diversamente; questa differenza API non e documentata ne testata in nessun file della suite compare_scripts; test_moshier_unsupported.py non testa ID negativi. IMPATTO: Ketu (nodo sud) e il punto piu importante in Jyotish dopo Rahu; se il comportamento di ID negativi differisce, applicazioni che usano -10 per Ketu (pattern comune in pyswisseph) otterranno risultati diversi; se pyswisseph non supporta -10 con Moshier ma libephemeris si, e una incompatibilita API critica per la migrazione. FIX: Testare body ID -10 e -11 con `swe.FLG_MOSEPH` in entrambe per 3 date; se pyswisseph lancia eccezione, catturare e documentare; se entrambi supportano, confrontare lon con angular_diff (tolleranza < 0.02 deg) e verificare lon_sud = lon_nord + 180; se il supporto differisce, marcare come xfail con nota. RISULTATO ATTESO: 6 test case documentano la compatibilita dei nodi sud Moshier, risolvendo un'ambiguita API critica per Jyotish.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_unsupported.py`: comparare il comportamento di errore per body non supportati in Moshier tra pyswisseph e libephemeris, estendendo `test_moshier_unsupported.py` con il confronto cross-library [`libephemeris/planets.py:830-839`]. PROBLEMA: `test_moshier/test_moshier_unsupported.py` (327 righe) verifica che libephemeris lanci `CalculationError` per 20+ body non supportati in Moshier; tuttavia non verifica che pyswisseph lanci la stessa eccezione o un'eccezione compatibile; pyswisseph potrebbe restituire un errore diverso (SwissephError con messaggio diverso, return code negativo, o risultato parziale con warning); la compatibilita dell'errore non e mai stata verificata cross-library. IMPATTO: Applicazioni che migrano da pyswisseph a libephemeris con try/except specifici per SwissephError non cattureranno CalculationError; il tipo e il messaggio dell'errore determinano il flusso dell'applicazione; una differenza nel tipo di eccezione causa crash non gestiti o fallback silenzioso errato. FIX: Per 8 body ID chiave (SE_CHIRON=15, SE_CERES=17, SE_PALLAS=18, SE_JUNO=19, SE_VESTA=20, SE_CUPIDO=40, SE_ERIS, SE_AST_OFFSET+433), chiamare calc_ut con `SEFLG_MOSEPH` in entrambe le implementazioni; catturare qualsiasi eccezione; documentare tipo, messaggio, e attributi; confrontare che gli stessi body siano rifiutati. RISULTATO ATTESO: 8 test case documentano la compatibilita degli errori Moshier cross-library per ogni categoria di body non supportato.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_range.py`: comparare i limiti del range Moshier tra pyswisseph e libephemeris, verificando che accettino/rifiutino le stesse date [`libephemeris/exceptions.py:1032`, `libephemeris/planets.py:823-825`]. PROBLEMA: libephemeris definisce MOSHIER_JD_START=625673.5 (~-2999 CE) e MOSHIER_JD_END=3182395.5 (~+4000 CE); la C library Swiss Ephemeris supporta Moshier per un range molto piu ampio (~-13200 a +17200 CE); questa discrepanza significa che date come -5000 CE o +10000 CE funzionano in pyswisseph ma lanciano EphemerisRangeError in libephemeris; i test in `test_moshier_range.py` (377 righe) testano solo il range di libephemeris senza confrontare con pyswisseph. IMPATTO: Utenti di archeologia astronomica per civilta antiche (Sumeria -4000, Egitto -3000) troveranno che pyswisseph funziona per queste date ma libephemeris le rifiuta; questa e una regressione funzionale critica per la migrazione; la documentazione "range -3000 to +3000 CE" e fuorviante se pyswisseph supporta -13200. FIX: Testare 15 date distribuite: 5 dentro il range libephemeris, 5 dentro il range C ma fuori libephemeris (es. -5000, -4000, +5000, +7000, +10000 CE), 5 fuori entrambi i range; per ogni data chiamare calc_ut con MOSEPH in entrambe; documentare accettazione/rifiuto; verificare che le stesse date accettate producano posizioni concordanti. RISULTATO ATTESO: 15 test case mappano completamente i range Moshier C vs Python, documentando la discrepanza e fornendo dati per decidere se allineare i limiti.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_retflag.py`: comparare il return flag (secondo elemento della tupla) di `swe.calc_ut()` e `ephem.swe_calc_ut()` con `SEFLG_MOSEPH` per varie combinazioni di flag [`libephemeris/planets.py:1084`]. PROBLEMA: libephemeris restituisce `iflag` invariato come return flag (planets.py:1084); la C library Swiss Ephemeris modifica il return flag per indicare quali flag sono stati effettivamente applicati (aggiungendo SEFLG_MOSEPH se usato, rimuovendo SEFLG_TOPOCTR se non supportato); test_compare_planets.py non verifica il return flag; questa discrepanza puo confondere applicazioni che controllano `retflag & SEFLG_MOSEPH` per determinare quale efferide e stata usata. IMPATTO: Il return flag e l'unico meccanismo per determinare a runtime quale efferide e stata effettivamente usata; applicazioni che implementano fallback automatico (es. prova SEFLG_SWIEPH, se fallisce prova SEFLG_MOSEPH, controlla retflag) dipendono dalla correttezza del return flag; discrepanze causano loop infiniti o fallback errati. FIX: Testare 8 combinazioni di flag: MOSEPH solo, MOSEPH|SPEED, MOSEPH|EQUATORIAL, MOSEPH|SIDEREAL, MOSEPH|HELCTR, MOSEPH|TOPOCTR, MOSEPH|ICRS, MOSEPH|J2000; per ognuna con 2 pianeti, confrontare return flag bit per bit; documentare quali bit differiscono tra C e Python. RISULTATO ATTESO: 16 test case documentano la compatibilita del return flag Moshier, identificando ogni discrepanza.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_topocentric.py`: comparare `swe.calc_ut(jd, planet, swe.FLG_MOSEPH | swe.FLG_TOPOCTR)` vs `ephem.swe_calc_ut(jd, planet, SEFLG_MOSEPH | SEFLG_TOPOCTR)` previa `set_topo()`, con xfail per il TODO alla riga 1049 di planets.py [`libephemeris/planets.py:1043-1049`]. PROBLEMA: Alla riga 1045-1049 di planets.py, la correzione topocentrica in Moshier ha un commento `# Moon parallax is significant (~1 degree max)` seguito da `pass  # TODO: Implement proper topocentric correction if needed`; questo significa che `SEFLG_TOPOCTR` e silenziosamente ignorato in Moshier; la C library applica la parallasse; per la Luna, la differenza e fino a ~1 grado; nessun test documenta questa mancanza ne avverte l'utente. IMPATTO: La parallasse lunare di ~1 grado rende la posizione della Luna inutilizzabile per eclissi locali, occultazioni, rise/set dalla superficie terrestre, e qualsiasi applicazione che richieda la posizione topocentrica; applicazioni che migrano da pyswisseph con `FLG_TOPOCTR | FLG_MOSEPH` otterranno posizioni lunari sbagliate di fino a 1 grado SENZA ALCUN WARNING; questa e la violazione piu grave della compatibilita 1:1. FIX: Creare test con `pytestmark = pytest.mark.xfail(reason="SEFLG_TOPOCTR silenziosamente ignorato in Moshier (planets.py:1049 TODO)", strict=False)`; impostare set_topo(12.5, 41.9, 0) in entrambe; testare Moon e Sun con `SEFLG_MOSEPH | SEFLG_TOPOCTR` per 3 date; confrontare risultati; documentare la differenza Moon topocentrica; aggiungere test per verificare che il return flag rifletta l'assenza di TOPOCTR. RISULTATO ATTESO: 6 test case xfail documentano la discrepanza topocentrica Moshier, servendo da roadmap per l'implementazione e avvertimento per gli utenti.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_1000_dates.py`: test slow con 1000 date random nel range DE440 overlap (1550-2650) comparando tutti i pianeti con `SEFLG_MOSEPH` tra pyswisseph e libephemeris, usando il pattern `random_dates_in_de440_range(n=1000, seed=42)` dal conftest.py [`libephemeris/planets.py:774-1084`]. PROBLEMA: I test puntuali su 5-10 date hanno coverage limitata e possono non rilevare errori che si manifestano solo per specifici valori di JD (singolarita nelle serie VSOP87, divisioni per zero nei coefficienti del Moshier, overflow numerico nei termini periodici di ELP); il conftest.py fornisce `random_dates_in_de440_range` (linea 448-463) ma nessun test Moshier lo usa; test_compare_planets.py non ha un equivalente massivo; serve un test statistico su campione ampio per garantire robustezza numerica. IMPATTO: Bug rari che si manifestano 1 volta su 1000 (es. divisione per zero quando un denominatore e prossimo a zero, overflow quando un angolo attraversa 360 deg) restano non rilevati; applicazioni che calcolano effemeridi continue per calendari o transiti su periodi di anni incontreranno errori sporadici; la fiducia nel porting richiede validazione statistica. FIX: Generare 1000 JD con seed=42 nel range 2287184.5-2688976.5; per ognuna calcolare Sun, Moon, Mars, Jupiter con `swe.FLG_MOSEPH | swe.FLG_SPEED` in entrambe; asserire lon < 0.01 deg, lat < 0.01 deg, speed < 0.02 deg/day per tutti; marcare `@pytest.mark.slow`; usare ProgressReporter dal conftest (linea 678-756) per output. RISULTATO ATTESO: 4000 confronti (1000 date x 4 pianeti) passano, dimostrando robustezza statistica; il test richiede ~5 minuti ma fornisce la copertura piu alta possibile.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_historical_birthcharts.py`: comparare oroscopi completi (pianeti + case + nodi + Lilith) per i personaggi famosi nel fixture `famous_people` del conftest.py (Einstein, Monroe, Gandhi, Picasso, Mandela) con `SEFLG_MOSEPH`, seguendo il pattern di `test_compare_observations.py` [`compare_scripts/tests/conftest.py:518-567`]. PROBLEMA: Il conftest.py fornisce il fixture `famous_people` con dati di nascita dettagliati (anno, mese, giorno, ora, lat, lon per 5 personaggi); `test_compare_observations.py` usa SUBJECTS per testare modalita osservative ma solo con JPL; un oroscopo completo richiede: 10 pianeti, 4 punti lunari, 12 cuspidi, Asc, MC; testare l'intero oroscopo con Moshier e il test di integrazione piu realistico perche replica esattamente cosa fa un software astrologico. IMPATTO: L'oroscopo completo e il prodotto finale visibile dall'utente; se anche un solo componente differisce tra C e Python (es. Ascendente in segno diverso, Luna in casa diversa), l'utente percepisce il software come "sbagliato"; questo test e il gold standard per la validazione end-to-end e per la comunicazione con gli utenti. FIX: Per ogni personaggio: calcolare JD, poi 10 pianeti + 4 punti lunari con `SEFLG_MOSEPH | SEFLG_SPEED`, poi cuspidi con `swe_houses_ex(jd, lat, lon, ord('P'), SEFLG_MOSEPH)`; confrontare tutti i componenti tra C e Python; tolleranze: pianeti < 0.01 deg, cuspidi < 0.02 deg, velocita < 0.02 deg/day; verificare che tutti i pianeti siano nella stessa casa. RISULTATO ATTESO: 5 oroscopi completi (5 x ~26 componenti = 130 confronti) validano l'integrazione end-to-end Moshier per casi reali astrologicamente significativi.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_combined_flags.py`: comparare risultati con combinazioni multiple di flag Moshier: `SEFLG_MOSEPH | SEFLG_SPEED | SEFLG_EQUATORIAL`, `SEFLG_MOSEPH | SEFLG_SIDEREAL | SEFLG_SPEED`, `SEFLG_MOSEPH | SEFLG_HELCTR | SEFLG_SPEED`, `SEFLG_MOSEPH | SEFLG_EQUATORIAL | SEFLG_SIDEREAL` [`libephemeris/planets.py:857-1082`]. PROBLEMA: Ogni flag aggiuntivo attiva un ramo diverso nel dispatcher `_calc_body_moshier()`; l'ordine di applicazione in libephemeris e: (1) calcolo posizioni raw, (2) heliocentric if HELCTR, (3) equatorial if EQUATORIAL, (4) sidereal if SIDEREAL and not EQUATORIAL; la C library potrebbe applicare le trasformazioni in ordine diverso; combinazioni come `EQUATORIAL | SIDEREAL` sono ambigue (libephemeris non applica sidereal se equatorial e attivo, riga 1072); test_compare_planets.py testa ICRS/J2000/NONUT con JPL ma non con Moshier; nessun test verifica combinazioni di 3+ flag. IMPATTO: Utenti avanzati combinano flag per casi d'uso specifici (es. velocita siderali eliocentriche per cosmobiologia vedica); interazioni non testate producono errori silenti impossibili da diagnosticare; la compatibilita 1:1 richiede che ogni combinazione legittima di flag produca gli stessi risultati. FIX: Definire 8 combinazioni di flag chiave; per ognuna testare Sun, Mars, Jupiter x 3 date; confrontare tutti i 6 componenti con tolleranze appropriate per ogni combinazione; documentare combinazioni illegittime o non supportate. RISULTATO ATTESO: 72 test case (8 flag x 3 pianeti x 3 date) coprono le permutazioni di flag piu comuni, confermando l'ordine di applicazione.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_pluto_dedicated.py`: test dedicato a Pluto Moshier con 20 date distribuite nel range 1550-2650, confrontando la teoria Chapront-Francou C vs Python, con report di precisione per epoca [`libephemeris/moshier/pluto.py:1-425`, `libephemeris/moshier/pluto_data.py:1-2993`]. PROBLEMA: Pluto e il pianeta piu problematico in Moshier: usa la teoria DE404-based di Chapront-Francou con 2993 righe di tabelle di perturbazione; il test test_moshier_vs_jpl.py ha tolleranza 60000 arcsec (16.7 deg!) per Pluto vs JPL, ma questo e Moshier-vs-JPL (errore della teoria); per Moshier-C-vs-Moshier-Python lo stesso algoritmo dovrebbe produrre risultati quasi identici (<10 arcsec) se le tabelle numeriche sono le stesse; il test test_moshier_pluto.py verifica solo internamente (lon~250, dist 28-50 AU); nessun confronto cross-library. IMPATTO: Pluto e il pianeta generazionale per eccellenza; l'interpretazione di Pluto in segno determina generazioni intere; un errore di 16 deg (tolleranza attuale) significa potenzialmente segno sbagliato; il confronto C-Python isola l'errore del porting dalle limitazioni della teoria, fornendo informazione critica per migliorare l'implementazione. FIX: Parametrizzare su 20 date ben distribuite (1550-2650 con densita maggiore vicino a J2000); per ogni data, confrontare lon/lat/dist tra `swe.calc_ut(jd, 9, swe.FLG_MOSEPH)` e `ephem.swe_calc_ut(jd, SE_PLUTO, SEFLG_MOSEPH)` con tolleranze strette: lon < 0.01 deg (36 arcsec), lat < 0.01 deg, dist < 0.1%; riportare errore massimo e medio; se fallisce, le tabelle numeriche del porting differiscono dal C. RISULTATO ATTESO: 20 test case con report di precisione Pluto C-vs-Python, distinguendo errori del porting (<0.01 deg attesi) da errori della teoria (fino a 16 deg vs JPL).

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_moon_dedicated.py`: test dedicato alla Luna (ELP 2000-82B) con 25 date distribuite nel range 1550-2650, testando tutti i 6 componenti incluse velocita, con date speciali vicino a perigeo/apogeo/nodi/eclissi [`libephemeris/moshier/elp82b.py:1-581`, `libephemeris/moshier/elp82b_data.py:1-690`]. PROBLEMA: La Luna usa ELP 2000-82B con 60+ termini di longitudine, 60+ di latitudine, 40+ di distanza e 10+ termini planetari (elp82b_data.py); le correzioni di eccentricita, il calcolo degli argomenti fondamentali (L', D, M, M', F) e le perturbazioni planetarie sono implementazioni complesse; test_elp82b_precision.py (408 righe) confronta ELP vs DE440 con tolleranza 300 arcsec; per ELP-C-vs-ELP-Python la tolleranza dovrebbe essere <5 arcsec se i coefficienti sono identici. IMPATTO: La Luna e il corpo celeste che si muove piu velocemente (~13 deg/day); errori nella Luna propagano a: True Node (calcolato dalla posizione lunare), eclissi, fasi lunari, calendari lunari, progressioni lunari; la velocita lunare determina quando la Luna cambia segno (ogni ~2.5 giorni) e casa (ogni ~2 ore); errori anche piccoli hanno impatto immediato e visibile. FIX: Creare 25 date: 5 a perigeo lunare (dist minima), 5 ad apogeo (dist massima), 5 ai nodi (lat=0), 5 a eclissi note, 5 a date casuali; confrontare lon, lat, dist, speed_lon, speed_lat, speed_dist con tolleranze: lon < 0.005 deg (18 arcsec), lat < 0.005 deg, dist rel < 0.5%, speed_lon < 0.02 deg/day (Moon speed ~13), speed_lat < 0.005 deg/day. RISULTATO ATTESO: 25 test case con focus su casi astronomicamente significativi validano l'implementazione ELP 2000-82B cross-library con sub-20-arcsec precision.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_earth.py`: comparare SE_EARTH (ID 14) in tutte le modalita (geocentrico, eliocentrico, con velocita) con `SEFLG_MOSEPH` tra pyswisseph e libephemeris [`libephemeris/planets.py:972-987`, `libephemeris/moshier/vsop87.py:calc_earth_heliocentric`]. PROBLEMA: SE_EARTH e un body speciale: geocentrico dovrebbe restituire (0,0,0) o errore; eliocentrico usa `calc_earth_heliocentric()` da VSOP87; Moon eliocentrica e approssimata come Earth (planets.py:958: "Moon's heliocentric position is ~same as Earth"); la C library potrebbe gestire SE_EARTH diversamente; pyswisseph potrebbe non esporre SE_EARTH con Moshier; test_calc_body_moshier.py non testa SE_EARTH. IMPATTO: SE_EARTH eliocentrica e la posizione della Terra nel sistema solare, base di tutte le conversioni geocentriche; un errore nella Terra produce un errore identico in tutti i pianeti geocentrici; l'approssimazione Moon~=Earth ha un errore massimo di ~0.0003 AU che potrebbe differire dalla C library. FIX: Testare SE_EARTH con 4 combinazioni: geocentrico puro, HELCTR, HELCTR|SPEED, HELCTR|EQUATORIAL per 3 date; documentare se pyswisseph supporta SE_EARTH con Moshier; se si, confrontare con tolleranza < 0.001 deg per lon, < 0.01% per dist; se geocentrico lancia errore, verificare errore identico. RISULTATO ATTESO: 12 test case documentano completamente il comportamento di SE_EARTH Moshier in entrambe le implementazioni.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_nod_aps.py`: file analogo alla sezione TestNodAps di `compare_scripts/tests/test_compare_orbital.py` ma con `SEFLG_MOSEPH`; comparare `swe.nod_aps_ut(jd, planet, swe.FLG_MOSEPH, method)` vs `ephem.swe_nod_aps_ut(jd, planet, SEFLG_MOSEPH, method)` per nodi e apsidi planetari [`libephemeris/planets.py:3584-3633`]. PROBLEMA: `test_compare_orbital.py` ha TestNodAps marcato xfail per differenze helio-vs-geo; nod_aps_ut con Moshier chiama internamente calc_ut con MOSEPH; la C library ha un'implementazione integrata; SE_NODBIT_MEAN(1) vs SE_NODBIT_OSCU(0) possono produrre risultati molto diversi; per pianeti con orbite eccentriche (Mercury, Pluto) i nodi osculanti oscillano significativamente. IMPATTO: I nodi planetari sono usati in astrologia evolutiva (nodo nord di Saturno, Pluto, Jupiter); le apsidi (perielio/afelio) determinano la distanza dal Sole e l'eccentricita orbitale; la mancanza di validazione Moshier per nod_aps rende queste funzioni inaffidabili per date storiche. FIX: Testare 5 pianeti (Mercury, Mars, Jupiter, Saturn, Uranus) x 2 metodi (MEAN, OSCU) x 3 date con `swe.FLG_MOSEPH`; per ogni chiamata verificare i 4 risultati (nodo_asc, nodo_desc, perielio, afelio) x 6 componenti; tolleranza lon < 0.1 deg; usare xfail se la C library produce errore per certi pianeti/metodi con Moshier. RISULTATO ATTESO: 30 test case documentano lo stato di supporto nod_aps Moshier cross-library con xfail per casi non supportati.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_precession.py`: comparare l'obliquita media e la nutazione calcolate dal modulo `moshier.precession` con i valori impliciti nella C library Swiss Ephemeris per le stesse date, derivati confrontando posizioni eclittiche vs equatoriali [`libephemeris/moshier/precession.py:1-826`]. PROBLEMA: libephemeris usa IAU 2006 per la precessione e IAU 2000B per la nutazione (76 termini, test_moshier_precession.py:636 righe); la C library Swiss Ephemeris usa Lieske 1979 per la precessione e IAU 2000B per la nutazione ma potrebbe avere implementazione diversa dei 76 termini; l'obliquita a J2000 e ~23.4393 deg in entrambe ma diverge per date lontane; nessun test confronta direttamente obliquita/nutazione C-vs-Python; la differenza si manifesta indirettamente in tutte le trasformazioni eclittica-equatoriale. IMPATTO: La precessione determina lo spostamento della frame eclittica nel tempo (~50.3 arcsec/anno); un errore di 1 arcsec nella precessione produce 1 arcsec di errore in ogni posizione planetaria; per date storiche (-2000 CE), 4000 anni x 1 arcsec/anno = 4000 arcsec di errore cumulativo; questo puo dominare l'errore totale nelle posizioni Moshier. FIX: Calcolare posizione eclittica e equatoriale dello stesso pianeta con `swe.FLG_MOSEPH` e `swe.FLG_MOSEPH | swe.FLG_EQUATORIAL` in entrambe le implementazioni per 10 date; derivare l'obliquita implicita dalla relazione ecl->eq; confrontare obliquita implicita C-vs-Python; confrontare anche con `moshier.mean_obliquity()` direttamente per le stesse date. RISULTATO ATTESO: 10 test case derivano e confrontano l'obliquita implicita nelle due implementazioni, quantificando la divergenza di precessione come funzione del tempo.

- [ ] Creare `compare_scripts/tests/test_moshier/test_moshier_compare_historical.py`: test dedicato a date storiche profonde (-2000, -1000, 0, 500, 1000, 1200 CE) per tutti i pianeti con `SEFLG_MOSEPH`, con report di precisione per epoca, usando il pattern di TestMoshierEdgeCases con tolleranze graduali [`libephemeris/planets.py:774-1084`, `libephemeris/moshier/vsop87.py:1-1138`]. PROBLEMA: Le date storiche sono il caso d'uso primario di Moshier (fuori DE440); le teorie VSOP87/ELP degradano per date lontane da J2000 con un tasso che puo differire tra C e Python a causa di: diversa precessione, diversa nutazione, diversi termini troncati nelle serie; test_moshier_vs_jpl.py testa solo nel range DE440 (1550-2650); nessun test confronta C-vs-Python per date realmente storiche; la degradazione e non-lineare e potrebbe essere molto diversa per pianeti diversi. IMPATTO: Archeologia astronomica, datazione astronomica di eventi storici (eclissi antiche, congiunzioni planetarie bibliche), astro-cronologia e astrologia storica dipendono dalla precisione Moshier per date antiche; senza confronto C-vs-Python per queste date, non si sa se libephemeris e utilizzabile per questi scopi critici. FIX: Parametrizzare su 8 date storiche x 10 pianeti; confrontare lon/lat/dist con tolleranze graduali: < 0.01 deg per 1000-1550 CE, < 0.05 deg per 0-1000 CE, < 0.2 deg per -1000-0 CE, < 0.5 deg per < -1000 CE; riportare errore massimo e medio per ogni pianeta e ogni epoca; marcare @pytest.mark.slow. RISULTATO ATTESO: 80 test case (8 x 10) producono una matrice di precisione [pianeta x epoca] C-vs-Python, documentando quale combinazione pianeta/epoca e affidabile per applicazioni storiche.
